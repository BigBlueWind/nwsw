/*
 * Note: this file originally auto-generated by mib2c using
 *  : mib2c.iterate.conf 17695 2009-07-21 12:22:18Z dts12 $
 */

#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

#include "bs_modules.h"
#include "all_cfg.h"
#include "all_cmn.h"
#include "tlv_enc_dec.h"

#include "sih.h"
#include "syslog.h"

#include "radioHeadConfig.h"

/* function declarations */
Netsnmp_Node_Handler radioHeadConfigTable_handler;
Netsnmp_First_Data_Point  radioHeadConfigTable_get_first_data_point;
Netsnmp_Next_Data_Point   radioHeadConfigTable_get_next_data_point;

/* globals */
radioHeadConfigTableEntry  *radioHeadConfigTable_head = NULL;
static TBOOL g_sihRadioHeadConfigSetReqProcessed = FALSE;
static TBOOL g_sihRadioHeadConfigGetReqProcessed = FALSE;
static UINT32 g_prevRadioHeadConfigReqCount = 0;
static UINT32 g_radioHeadConfigFirstCall = 0;

/* free_radioHeadConfigTable */
/**
* @details 
*          This is the routine which frees the radioHead config entry. 
*/
void free_radioHeadConfigTable(void)
{
  radioHeadConfigTableEntry  * current = radioHeadConfigTable_head;

  if (current) 
  {
  for (current = radioHeadConfigTable_head->next ; current != NULL; current = current->next) {
    SNMP_FREE( current );  
    }
   
  SNMP_FREE(radioHeadConfigTable_head);
  radioHeadConfigTable_head = NULL;
  }
  return;
}


/**
* @details 
*          This is the routine which handles get response from SIH Get Processor
*          and prepare the SNMP GET response for EMS. 
* @param tlvArray contains pointer to TLV array.     
*/
TBOOL sendRadioHeadConfigSnmpGetResponse (
  IO TlvArray    *tlvArray
  )
{
  int  totalNumRow, totalNumCol, i=0, colmn=0;
  radioHeadConfigTableEntry * prevPtr = NULL;

  printTLV(tlvArray);

  /* Total no. of Colmns */
  totalNumCol = RADIOHEAD_CONFIG_MAX_COL;
  /* Total no. of Row = (No. of TLV)/(No. of Columns) */
  totalNumRow = tlvArray->numTlvs/totalNumCol;

  for (i=1;  i <= totalNumRow;  i++) 
      {
      radioHeadConfigTableEntry *ptr=(radioHeadConfigTableEntry *)malloc(sizeof(radioHeadConfigTableEntry));

      clearBytes(ptr,sizeof(radioHeadConfigTableEntry));

      /* Fill Data in TLV */
      for(colmn = 1 ; colmn <= totalNumCol ; colmn++)
        {
        if (colmn EQ COLUMN_RADIOHEADPROFILEID)
            {
             ptr->radioHeadProfileId = getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
             continue;
            }        
        if (colmn EQ COLUMN_TXATTENPATHA)
            {
             ptr->txAttenPathA = getUint16FromArray(tlvArray->tlvInfo[colmn-1].data);
             continue;
            }        
        if (colmn EQ COLUMN_TXATTENPATHB)
            {
             ptr->txAttenPathB = getUint16FromArray(tlvArray->tlvInfo[colmn-1].data);
             continue;
            }
        if (colmn EQ COLUMN_RADIOHEADCONFIG_TXDELAY)
            {
             ptr->txDelay = getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
             continue;
            }       
        if (colmn EQ COLUMN_RADIOHEADCONFIG_RXDELAY)
            {
             ptr->rxDelay = getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
             continue;
            }       
        if (colmn EQ COLUMN_VSWRSHUTDOWNALARMTHRESHOLD)
            {
             ptr->vswrShutdownAlarmThreshold = getUint8FromArray(tlvArray->tlvInfo[colmn-1].data);
             continue;
            }        
        if (colmn EQ COLUMN_OVERPOWERALARMTHRESHOLD)
            {
             ptr->overpowerAlarmThreshold = getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
             continue;
            }        
        if (colmn EQ COLUMN_LOWPOWERALARMTHRESHOLD)
            {
             ptr->lowpowerAlarmThreshold = getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
             continue;
            }        
        }

       ptr->next = NULL;

      if (prevPtr == NULL) {
           radioHeadConfigTable_head =  prevPtr = ptr;
       }

      prevPtr->next = ptr;
      prevPtr = ptr;
     }
    prevPtr->next = NULL;

    if (radioHeadConfigTable_head)
        return TRUE;
    else
        return FALSE;
}

/**
* @details 
*          This routine sends radioHead config set request to BSM, it uses SIH Set 
*          Processor to create and send the message to BSM. 
* @param sihTagInfo is used to have the tag information for set request.
*/
TBOOL sendRadioHeadConfigSnmpSetRequest ( 
      IO TagInfo  sihTagInfo,
      IO TlvArray *tlvArray
      )
{
   radioHeadConfigTableEntry * setReqData = NULL;   
   TBOOL status = FALSE;
   int colmn = 0;
 
   setReqData = radioHeadConfigTable_head;

   /* Fill Data in TLV */
   for(colmn = 0; colmn < tlvArray->numTlvs; colmn++)
   {
         if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_RADIOHEADPROFILEID)
         {
           putUint32InArray( setReqData->radioHeadProfileId, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->radioHeadProfileId);     
         }
         if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_TXATTENPATHA)
         {
           putUint16InArray( setReqData->txAttenPathA, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->txAttenPathA);     
         }
         if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_TXATTENPATHB)
         {
           putUint16InArray( setReqData->txAttenPathB, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->txAttenPathB);
         }
         if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_RADIOHEADCONFIG_TXDELAY)
         {
           putUint8InArray( setReqData->txDelay, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->txDelay); 
         }
         if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_RADIOHEADCONFIG_RXDELAY)
         {
           putUint8InArray( setReqData->rxDelay, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->rxDelay);     
         }
         if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_VSWRSHUTDOWNALARMTHRESHOLD)
         {
           putUint8InArray( setReqData->vswrShutdownAlarmThreshold, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->vswrShutdownAlarmThreshold);     
         }       
         if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_OVERPOWERALARMTHRESHOLD)
         {
           putUint32InArray( setReqData->overpowerAlarmThreshold, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->overpowerAlarmThreshold);     
         }       
         if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_LOWPOWERALARMTHRESHOLD)
         {
           putUint32InArray( setReqData->lowpowerAlarmThreshold, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->lowpowerAlarmThreshold);     
         }       
   }

   /* Invoke SIH Set Processor */
   status = sihSetProcessor (sihTagInfo,  tlvArray);
   if (status EQ TRUE)
     {
      g_sihRadioHeadConfigSetReqProcessed = TRUE;
      g_sihRadioHeadConfigGetReqProcessed = FALSE;
      /* free the resources */      
      free_radioHeadConfigTable(); 
     }
   return status;
}


/**
* @details 
*          This is the routine for initializing radioHeadConfig module.
*/
void
init_radioHeadConfig(void)
{
  /* here we initialize all the tables we're planning on supporting */
    initialize_table_radioHeadConfigTable();
}



/** Initialize the radioHeadConfigTable table by defining its contents and how it's structured */
void
initialize_table_radioHeadConfigTable(void)
{
    const oid radioHeadConfigTable_oid[] = {1,3,6,1,4,1,9344,501,RADIOHEADCONFIG,1};
    const size_t radioHeadConfigTable_oid_len   = OID_LENGTH(radioHeadConfigTable_oid);
    netsnmp_handler_registration    *reg;
    netsnmp_iterator_info           *iinfo;
    netsnmp_table_registration_info *table_info;

    DEBUGMSGTL(("radioHeadConfig:init", "initializing table radioHeadConfigTable\n"));

    reg = netsnmp_create_handler_registration(
              "radioHeadConfigTable",     radioHeadConfigTable_handler,
              radioHeadConfigTable_oid, radioHeadConfigTable_oid_len,
              HANDLER_CAN_RWRITE
              );

    table_info = SNMP_MALLOC_TYPEDEF( netsnmp_table_registration_info );
    netsnmp_table_helper_add_indexes(table_info,
                           ASN_UNSIGNED,  /* index: radioHeadProfileId */
                           0);
    table_info->min_column = COLUMN_RADIOHEADPROFILEID;
    table_info->max_column = COLUMN_LOWPOWERALARMTHRESHOLD;
    
    iinfo = SNMP_MALLOC_TYPEDEF( netsnmp_iterator_info );
    iinfo->get_first_data_point = radioHeadConfigTable_get_first_data_point;
    iinfo->get_next_data_point  = radioHeadConfigTable_get_next_data_point;
    iinfo->table_reginfo        = table_info;
    
    netsnmp_register_table_iterator( reg, iinfo );

    /* Initialise the contents of the table here */
}


#ifndef PWAV_SIH 
/* create a new row in the (unsorted) table */
struct radioHeadConfigTable_entry *
radioHeadConfigTable_createEntry(
                 u_long  radioHeadProfileId,
                ) {
    struct radioHeadConfigTable_entry *entry;

    entry = SNMP_MALLOC_TYPEDEF(struct radioHeadConfigTable_entry);
    if (!entry)
        return NULL;

    entry->radioHeadProfileId = radioHeadProfileId;
    entry->next = radioHeadConfigTable_head;
    radioHeadConfigTable_head = entry;
    return entry;
}

/* remove a row from the table */
void
radioHeadConfigTable_removeEntry( struct radioHeadConfigTable_entry *entry ) {
    struct radioHeadConfigTable_entry *ptr, *prev;

    if (!entry)
        return;    /* Nothing to remove */

    for ( ptr  = radioHeadConfigTable_head, prev = NULL;
          ptr != NULL;
          prev = ptr, ptr = ptr->next ) {
        if ( ptr == entry )
            break;
    }
    if ( !ptr )
        return;    /* Can't find it */

    if ( prev == NULL )
        radioHeadConfigTable_head = ptr->next;
    else
        prev->next = ptr->next;

    SNMP_FREE( entry );   /* XXX - release any other internal resources */
}
#endif /* PWAV_SIH */

/* Example iterator hook routines - using 'get_next' to do most of the work */
netsnmp_variable_list *
radioHeadConfigTable_get_first_data_point(void **my_loop_context,
                          void **my_data_context,
                          netsnmp_variable_list *put_index_data,
                          netsnmp_iterator_info *mydata)
{
    TagInfo sihTagInfo;
    TBOOL status = FALSE;

    if (g_sihRadioHeadConfigGetReqProcessed EQ FALSE)
        {
        /* Set SIH Get Request Tag Info */
        sihTagInfo.tableId = RADIOHEADCONFIG;
        sihTagInfo.rowId = CONFIG_ROW_ID_NONE; 
        sihTagInfo.colId = CONFIG_COL_ID_NONE;                

        status = sihGetProcessor (sihTagInfo);
        if ( status EQ TRUE )
            {
            logInfo("%s","Get Processor Succedded");
            if (radioHeadConfigTable_head)
                {
		g_radioHeadConfigFirstCall = 1;
                g_sihRadioHeadConfigGetReqProcessed = TRUE;
                }
            }
        else
            logError("%s","Get Processor Failed");
        }        
    
    *my_loop_context = radioHeadConfigTable_head;
    return radioHeadConfigTable_get_next_data_point(my_loop_context, my_data_context,
                                    put_index_data,  mydata );
}

netsnmp_variable_list *
radioHeadConfigTable_get_next_data_point(void **my_loop_context,
                          void **my_data_context,
                          netsnmp_variable_list *put_index_data,
                          netsnmp_iterator_info *mydata)
{
    struct radioHeadConfigTable_entry *entry = (struct radioHeadConfigTable_entry *)*my_loop_context;
    netsnmp_variable_list *idx = put_index_data;

    if ( entry ) {
        snmp_set_var_typed_integer( idx, ASN_UNSIGNED, entry->radioHeadProfileId );
        idx = idx->next_variable;
        *my_data_context = (void *)entry;
        *my_loop_context = (void *)entry->next;
        return put_index_data;
    } else {
        return NULL;
    }
}


/** handles requests for the radioHeadConfigTable table */
int
radioHeadConfigTable_handler(
    netsnmp_mib_handler               *handler,
    netsnmp_handler_registration      *reginfo,
    netsnmp_agent_request_info        *reqinfo,
    netsnmp_request_info              *requests) {

    netsnmp_request_info       *request = NULL;
    netsnmp_table_request_info *table_info = NULL;
    struct radioHeadConfigTable_entry     *table_entry = NULL;
    int loopCount = 0;
    int ret;
    TagInfo sihTagInfo;
    TlvArray tlvArray;
    int rowIndex = 0, colId = 0;
    TBOOL status = FALSE;    


    DEBUGMSGTL(("radioHeadConfig:handler", "Processing request (%d)\n", reqinfo->mode));

    switch (reqinfo->mode) {
        /*
         * Read-support (also covers GetNext requests)
         */
    case MODE_GET:
        for (request=requests; request; request=request->next) {

            loopCount++;
            if (request->processed != 0)
            {
	     g_prevRadioHeadConfigReqCount = 0;
             g_sihRadioHeadConfigGetReqProcessed = FALSE;

             /* free the resources */
             free_radioHeadConfigTable(); 
             /* Here loopCount is decremented, Due to calling of free function two times in SNMP-GETNEXT
              * on single parameter -- 20/07/11 */
             loopCount--;
             continue;
            }

            /* perform anything here that you need to do before each
               request is processed. */           

            /* the following extracts the my_data_context pointer set in
               the loop functions above.  You can then use the results to
               help return data for the columns of the radioHeadConfigTable table 
               in question */ 

            table_entry = (struct radioHeadConfigTable_entry *)
                              netsnmp_extract_iterator_context(request);

            if ( table_entry == NULL) {
                if (reqinfo->mode == MODE_GET) {
                    netsnmp_set_request_error(reqinfo, request,SNMP_NOSUCHINSTANCE);
                    continue;
                }
                /* XXX: no row existed, if you support creation and this is a
                   set, start dealing with it here, else continue */
            }

            table_info  =     netsnmp_extract_table_info(request);
    
            /* table_info->colnum contains the column number requested */
            /* table_info->indexes contains a linked list of snmp variable
               bindings for the indexes of the table.  Values in the list
               have been set corresponding to the indexes of the
               request */
            if (table_info==NULL) {
                continue;
            }             

            switch (table_info->colnum) {
            case COLUMN_RADIOHEADPROFILEID:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_UNSIGNED,
                                            table_entry->radioHeadProfileId);
                break;
            case COLUMN_TXATTENPATHA:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_UNSIGNED,
                                            table_entry->txAttenPathA);
                break;
            case COLUMN_TXATTENPATHB:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_UNSIGNED,
                                            table_entry->txAttenPathB);
                break;
            case COLUMN_RADIOHEADCONFIG_TXDELAY:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_UNSIGNED,
                                            table_entry->txDelay);
                break;
            case COLUMN_RADIOHEADCONFIG_RXDELAY:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_UNSIGNED,
                                            table_entry->rxDelay);
                break;
            case COLUMN_VSWRSHUTDOWNALARMTHRESHOLD:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_UNSIGNED,
                                            table_entry->vswrShutdownAlarmThreshold);
                break;
            case COLUMN_OVERPOWERALARMTHRESHOLD:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_UNSIGNED,
                                            table_entry->overpowerAlarmThreshold);
                break;
            case COLUMN_LOWPOWERALARMTHRESHOLD:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_UNSIGNED,
                                            table_entry->lowpowerAlarmThreshold);
                break;
            default:
                netsnmp_set_request_error(reqinfo, request,
                                          SNMP_NOSUCHOBJECT);
                break;
            }
        }
	if(g_radioHeadConfigFirstCall EQ 1)
         {
            g_radioHeadConfigFirstCall = 0;
            g_prevRadioHeadConfigReqCount = loopCount;
            // This will occur in case of, when SNMP-GET or SNMP-GETNEXT of Single Parameter occurred.
            // In the first call itself loopCount becomes one.
            // In General Case, the loopCount will be always > 1 because every table contains 
            // index column to fetch the row and remaining columns contain table attributes.
            if(loopCount EQ 1)
            {
               //Setting it to false in case of SNMP-GET request for single parameter.
               g_sihRadioHeadConfigGetReqProcessed = FALSE;
               // Free all resources.
               free_radioHeadConfigTable();
            }
         }
         else if(loopCount > 1 || g_prevRadioHeadConfigReqCount > 1)
         {
            //update g_prevReqCount with current loopCount for next call of Table_handler
            g_prevRadioHeadConfigReqCount = loopCount;
         }
         else if(loopCount EQ 1)
         {
            //Setting it to false in case of SNMP-GET request for single parameter.
            g_sihRadioHeadConfigGetReqProcessed = FALSE;
            // Free all resources.
            free_radioHeadConfigTable();
         }
        break;

        /*
         * Write-support
         */
    case MODE_SET_RESERVE1:
#ifndef PWAV_SIH        
        for (request=requests; request; request=request->next) {
            table_entry = (struct radioHeadConfigTable_entry *)
                              netsnmp_extract_iterator_context(request);
            table_info  =     netsnmp_extract_table_info(      request);
    
            switch (table_info->colnum) {
            case COLUMN_RADIOHEADPROFILEID:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_TXATTENPATHA:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_TXATTENPATHB:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_FA1RXATTENPATHA:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_FA1RXATTENPATHB:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_RSSITHRESHOLD:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_VSWRSHUTDOWNALARMTHRESHOLD:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_OVERPOWERMINORALARMTHRESHOLD:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_LOWPOWERALARMTHRESHOLD:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            default:
                netsnmp_set_request_error( reqinfo, request,
                                           SNMP_ERR_NOTWRITABLE );
                return SNMP_ERR_NOERROR;
            }
        }
#endif /* PWAV_SIH */        
        break;

    case MODE_SET_RESERVE2:
        break;

    case MODE_SET_FREE:
        break;

    case MODE_SET_ACTION:
        g_sihRadioHeadConfigSetReqProcessed = FALSE; 

        clearBytes(&sihTagInfo, sizeof(TagInfo));
        clearBytes(&tlvArray, sizeof(TlvArray));
        
        for (request=requests; request; request=request->next) {
            table_entry = (struct radioHeadConfigTable_entry *)
                              netsnmp_extract_iterator_context(request);
            table_info  =     netsnmp_extract_table_info(      request);

            if(table_entry == NULL){
                logInfo(" %s\n", "table_entry is NULL");
                if (reqinfo->mode == MODE_SET_ACTION) {
                    netsnmp_set_request_error(reqinfo, request,SNMP_NOSUCHINSTANCE);
                    continue;
                }
                /* XXX: no row existed, if you support creation and this is a
                   set, start dealing with it here, else continue */
            }

            if(table_info == NULL){
                logInfo(" %s\n", "table_info is NULL");
                continue;
            }
    
            colId = table_info->colnum;
            /* set the row index */
            rowIndex = requests->index;

            switch (table_info->colnum) {
            case COLUMN_RADIOHEADPROFILEID:
                table_entry->radioHeadProfileId     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, RADIOHEADCONFIG, rowIndex, COLUMN_RADIOHEADPROFILEID);
                tlvArray.numTlvs++;
                break;
            case COLUMN_TXATTENPATHA:
                table_entry->txAttenPathA     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, RADIOHEADCONFIG, rowIndex, COLUMN_TXATTENPATHA);
                tlvArray.numTlvs++;
                break;
            case COLUMN_TXATTENPATHB:
                table_entry->txAttenPathB     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, RADIOHEADCONFIG, rowIndex, COLUMN_TXATTENPATHB);
                tlvArray.numTlvs++;
                break;
            case COLUMN_RADIOHEADCONFIG_TXDELAY:
                table_entry->txDelay     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, RADIOHEADCONFIG, rowIndex, COLUMN_RADIOHEADCONFIG_TXDELAY);
                tlvArray.numTlvs++;
                break;
            case COLUMN_RADIOHEADCONFIG_RXDELAY:
                table_entry->rxDelay     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, RADIOHEADCONFIG, rowIndex, COLUMN_RADIOHEADCONFIG_RXDELAY);
                tlvArray.numTlvs++;
                break;
            case COLUMN_VSWRSHUTDOWNALARMTHRESHOLD:
                table_entry->vswrShutdownAlarmThreshold     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, RADIOHEADCONFIG, rowIndex, COLUMN_VSWRSHUTDOWNALARMTHRESHOLD);
                tlvArray.numTlvs++;
                break;
            case COLUMN_OVERPOWERALARMTHRESHOLD:
                table_entry->overpowerAlarmThreshold     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, RADIOHEADCONFIG, rowIndex, COLUMN_OVERPOWERALARMTHRESHOLD);
                tlvArray.numTlvs++;
                break;
            case COLUMN_LOWPOWERALARMTHRESHOLD:
                table_entry->lowpowerAlarmThreshold     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, RADIOHEADCONFIG, rowIndex, COLUMN_LOWPOWERALARMTHRESHOLD);
                tlvArray.numTlvs++;
                break;
            }
        }

        if ((request == NULL) && (g_sihRadioHeadConfigSetReqProcessed EQ FALSE))
            /* Processed all requests and table entry is filled with
             * requested data. */
            {
            /* Set SIH Set Request Tag Info */
            sihTagInfo.tableId = RADIOHEADCONFIG;

            sihTagInfo.rowId = rowIndex; 

            status = sendRadioHeadConfigSnmpSetRequest (sihTagInfo, &tlvArray);
            if (status EQ TRUE)
                {
                syslog (LOG_INFO, "SET Succedded");
                }
            else
                netsnmp_set_request_error(reqinfo, requests, SNMP_ERR_GENERR );
            }
        break;

    case MODE_SET_UNDO:
#ifndef PWAV_SIH 
        for (request=requests; request; request=request->next) {
            table_entry = (struct radioHeadConfigTable_entry *)
                              netsnmp_extract_iterator_context(request);
            table_info  =     netsnmp_extract_table_info(      request);
    
            switch (table_info->colnum) {
            case COLUMN_RADIOHEADPROFILEID:
                table_entry->radioHeadProfileId     = table_entry->old_radioHeadProfileId;
                table_entry->old_radioHeadProfileId = 0;
                break;
            case COLUMN_TXATTENPATHA:
                table_entry->txAttenPathA     = table_entry->old_txAttenPathA;
                table_entry->old_txAttenPathA = 0;
                break;
            case COLUMN_TXATTENPATHB:
                table_entry->txAttenPathB     = table_entry->old_txAttenPathB;
                table_entry->old_txAttenPathB = 0;
                break;
            case COLUMN_FA1RXATTENPATHA:
                table_entry->fa1RxAttenPathA     = table_entry->old_fa1RxAttenPathA;
                table_entry->old_fa1RxAttenPathA = 0;
                break;
            case COLUMN_FA1RXATTENPATHB:
                table_entry->fa1RxAttenPathB     = table_entry->old_fa1RxAttenPathB;
                table_entry->old_fa1RxAttenPathB = 0;
                break;
            case COLUMN_RSSITHRESHOLD:
                table_entry->rssiThreshold     = table_entry->old_rssiThreshold;
                table_entry->old_rssiThreshold = 0;
                break;
            case COLUMN_VSWRSHUTDOWNALARMTHRESHOLD:
                table_entry->vswrShutdownAlarmThreshold     = table_entry->old_vswrShutdownAlarmThreshold;
                table_entry->old_vswrShutdownAlarmThreshold = 0;
                break;
            case COLUMN_OVERPOWERMINORALARMTHRESHOLD:
                table_entry->overpowerMinorAlarmThreshold     = table_entry->old_overpowerMinorAlarmThreshold;
                table_entry->old_overpowerMinorAlarmThreshold = 0;
                break;
            case COLUMN_LOWPOWERALARMTHRESHOLD:
                table_entry->lowpowerAlarmThreshold     = table_entry->old_lowpowerAlarmThreshold;
                table_entry->old_lowpowerAlarmThreshold = 0;
                break;
            }
        }
#endif /* PWAV_SIH */
        break;

    case MODE_SET_COMMIT:
        break;
    }
    return SNMP_ERR_NOERROR;
}
