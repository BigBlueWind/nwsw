/*
 * Note: this file originally auto-generated by mib2c using
 *  : mib2c.iterate.conf 19302 2010-08-13 12:19:42Z dts12 $
 */

#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

#include "bs_modules.h"
#include "all_cfg.h"
#include "all_cmn.h"
#include "tlv_enc_dec.h"

#include "sih.h"
#include "syslog.h"
#include "s1apGenCounters.h"

/* function declarations */
Netsnmp_Node_Handler s1apGenCountersTable_handler;
Netsnmp_First_Data_Point  s1apGenCountersTable_get_first_data_point;
Netsnmp_Next_Data_Point   s1apGenCountersTable_get_next_data_point;

/* globals */
struct s1apGenCountersTable_entry  *s1apGenCountersTable_head = NULL;
static TBOOL g_sihS1apGenCountersGetReqProcessed = FALSE;
static UINT32 g_prevS1apGenCountersReqCount = 0;
static UINT32 g_s1apGenCountersFirstCall = 0;

/* free_s1apGenCountersTable */
/**
* @details 
*          This is the routine which frees the sw package status entry.
*/
void free_s1apGenCountersTable(void)
{
  s1apGenCountersTableEntry  * current = s1apGenCountersTable_head;

  if (current) 
  {
  for (current = s1apGenCountersTable_head->next ; current != NULL; current = current->next) {
    SNMP_FREE( current );  
    }
   
    SNMP_FREE(s1apGenCountersTable_head);
    s1apGenCountersTable_head = NULL;
  }

  return;
}


/**
* @details 
*          This is the routine which handles get response from SIH Get Processor
*          and prepare the SNMP GET response for EMS.
* @param tlvArray contains pointer to TLV array.
*/
TBOOL sendS1apGenCountersSnmpGetResponse (
  IO TlvArray    *tlvArray
  )
{
  int  totalNumRow, totalNumCol, i=0, colmn=0;
  struct s1apGenCountersTable_entry * prevPtr = NULL;

  printTLV(tlvArray);

  /* Total no. of Colmns */
  totalNumCol = S1AP_GEN_COUNTERS_MAX_COL;
  /* Total no. of Row = (No. of TLV)/(No. of Columns) */
  totalNumRow = tlvArray->numTlvs/totalNumCol;

  logInfo("Total No. of Row %d & Col %d", totalNumRow, totalNumCol);
  for (i=1;  i <= totalNumRow;  i++)
      {
      struct s1apGenCountersTable_entry *ptr=(struct s1apGenCountersTable_entry *)malloc(sizeof(struct s1apGenCountersTable_entry));

      clearBytes (ptr, sizeof(struct s1apGenCountersTable_entry));

      /* Fill Data in TLV */
      for(colmn = 1 ; colmn <= totalNumCol ; colmn++)
        {
        if (colmn EQ COLUMN_S1AP_GEN_COUNTERS_INDEX)
           {
            ptr->index =  getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }
        if (colmn EQ COLUMN_NMBCONN)
           {
            ptr->nmbConn =  getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }
        if (colmn EQ COLUMN_NMBFAILCONN)
           {
            ptr->nmbFailConn =  getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }
        if (colmn EQ COLUMN_MAXMEMALLOC)
           {
            ptr->maxMemAlloc =  getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }
        if (colmn EQ COLUMN_NMBDECERR)
           {
            ptr->nmbDecErr =  getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }
        if (colmn EQ COLUMN_NMBENCERR)
           {
            ptr->nmbEncErr = getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }
        if (colmn EQ COLUMN_NMBAUDREQ)
           {
            ptr->nmbAudReq =  getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }
        if (colmn EQ COLUMN_NMBAUDCONN)
           {
            ptr->nmbAudConn =  getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }
        if (colmn EQ COLUMN_NMBAUDPEER)
           {
            ptr->nmbAudPeer =  getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }
        if (colmn EQ COLUMN_NMBAUDFLC)
           {
            ptr->nmbAudFlc =  getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }
	if (colmn EQ COLUMN_S1AP_GEN_COUNTERS_LASTCOLLECTEDAT)
           {
            clearBytes (ptr->lastCollectedAt, S1AP_GEN_COUNTERS_LCOLLECT_BUF_LEN_MAX);
            copyBytes (tlvArray->tlvInfo[colmn-1].data, tlvArray->tlvInfo[colmn-1].len, ptr->lastCollectedAt);
            ptr->lastCollectedAt_len = strlen(ptr->lastCollectedAt);
            continue;
           }
        }

       ptr->next = NULL;

      if (prevPtr == NULL) {
           s1apGenCountersTable_head =  prevPtr = ptr;
       }

      prevPtr->next = ptr;
      prevPtr = ptr;
     }
    prevPtr->next = NULL;

    if (s1apGenCountersTable_head)
        return TRUE;
    else
        return FALSE;
}



/** Initializes the s1apGenCounters module */
void
init_s1apGenCounters(void)
{
  /* here we initialize all the tables we're planning on supporting */
    initialize_table_s1apGenCountersTable();
}

  /* Determine the first/last column names*/

/** Initialize the s1apGenCountersTable table by defining its contents and how it's structured */
void
initialize_table_s1apGenCountersTable(void)
{
    const oid s1apGenCountersTable_oid[] = {1,3,6,1,4,1,9344,501,S1APGENCOUNTERS,1};
    const size_t s1apGenCountersTable_oid_len   = OID_LENGTH(s1apGenCountersTable_oid);
    netsnmp_handler_registration    *reg;
    netsnmp_iterator_info           *iinfo;
    netsnmp_table_registration_info *table_info;

    DEBUGMSGTL(("s1apGenCounters:init", "initializing table s1apGenCountersTable\n"));

    reg = netsnmp_create_handler_registration(
              "s1apGenCountersTable",     s1apGenCountersTable_handler,
              s1apGenCountersTable_oid, s1apGenCountersTable_oid_len,
              HANDLER_CAN_RONLY
              );

    table_info = SNMP_MALLOC_TYPEDEF( netsnmp_table_registration_info );
    netsnmp_table_helper_add_indexes(table_info,
                           ASN_UNSIGNED,  /* index: index */
                           0);
    table_info->min_column = COLUMN_S1AP_GEN_COUNTERS_INDEX;
    table_info->max_column = COLUMN_S1AP_GEN_COUNTERS_LASTCOLLECTEDAT;
    
    iinfo = SNMP_MALLOC_TYPEDEF( netsnmp_iterator_info );
    iinfo->get_first_data_point = s1apGenCountersTable_get_first_data_point;
    iinfo->get_next_data_point  = s1apGenCountersTable_get_next_data_point;
    iinfo->table_reginfo        = table_info;
    
    netsnmp_register_table_iterator( reg, iinfo );

    /* Initialise the contents of the table here */
}


#ifndef PWAV_SIH
/* create a new row in the (unsorted) table */
struct s1apGenCountersTable_entry *
s1apGenCountersTable_createEntry(
                 u_long  index,
                ) {
    struct s1apGenCountersTable_entry *entry;

    entry = SNMP_MALLOC_TYPEDEF(struct s1apGenCountersTable_entry);
    if (!entry)
        return NULL;

    entry->index = index;
    entry->next = s1apGenCountersTable_head;
    s1apGenCountersTable_head = entry;
    return entry;
}

/* remove a row from the table */
void
s1apGenCountersTable_removeEntry( struct s1apGenCountersTable_entry *entry ) {
    struct s1apGenCountersTable_entry *ptr, *prev;

    if (!entry)
        return;    /* Nothing to remove */

    for ( ptr  = s1apGenCountersTable_head, prev = NULL;
          ptr != NULL;
          prev = ptr, ptr = ptr->next ) {
        if ( ptr == entry )
            break;
    }
    if ( !ptr )
        return;    /* Can't find it */

    if ( prev == NULL )
        s1apGenCountersTable_head = ptr->next;
    else
        prev->next = ptr->next;

    SNMP_FREE( entry );   /* XXX - release any other internal resources */
}
#endif /* PWAV_SIH */


/* Example iterator hook routines - using 'get_next' to do most of the work */
netsnmp_variable_list *
s1apGenCountersTable_get_first_data_point(void **my_loop_context,
                          void **my_data_context,
                          netsnmp_variable_list *put_index_data,
                          netsnmp_iterator_info *mydata)
{
    TagInfo sihTagInfo;
    TBOOL status = FALSE;


    if (g_sihS1apGenCountersGetReqProcessed EQ FALSE)
        {
        /* Set SIH Get Request Tag Info */
        sihTagInfo.tableId = S1APGENCOUNTERS;
        sihTagInfo.rowId = CONFIG_ROW_ID_NONE;
        sihTagInfo.colId = CONFIG_COL_ID_NONE;

        status = sihGetProcessor (sihTagInfo);
        if ( status EQ TRUE )
            {
            logInfo("%s","Get Processor Succedded");
            if (s1apGenCountersTable_head)
                {
                g_s1apGenCountersFirstCall = 1;
                g_sihS1apGenCountersGetReqProcessed = TRUE;
                }
            }
        else
            logError("%s","Get Processor Failed");
        }

    *my_loop_context = s1apGenCountersTable_head;
    return s1apGenCountersTable_get_next_data_point(my_loop_context, my_data_context,
                                    put_index_data,  mydata );
}

netsnmp_variable_list *
s1apGenCountersTable_get_next_data_point(void **my_loop_context,
                          void **my_data_context,
                          netsnmp_variable_list *put_index_data,
                          netsnmp_iterator_info *mydata)
{
    struct s1apGenCountersTable_entry *entry = (struct s1apGenCountersTable_entry *)*my_loop_context;
    netsnmp_variable_list *idx = put_index_data;

    if ( entry ) {
        snmp_set_var_typed_integer( idx, ASN_UNSIGNED, entry->index );
        idx = idx->next_variable;
        *my_data_context = (void *)entry;
        *my_loop_context = (void *)entry->next;
        return put_index_data;
    } else {
        return NULL;
    }
}


/** handles requests for the s1apGenCountersTable table */
int
s1apGenCountersTable_handler(
    netsnmp_mib_handler               *handler,
    netsnmp_handler_registration      *reginfo,
    netsnmp_agent_request_info        *reqinfo,
    netsnmp_request_info              *requests) {

    netsnmp_request_info       *request = NULL;
    netsnmp_table_request_info *table_info = NULL;
    struct s1apGenCountersTable_entry          *table_entry = NULL;
    int loopCount = 0;

    DEBUGMSGTL(("s1apGenCounters:handler", "Processing request (%d)\n", reqinfo->mode));

    switch (reqinfo->mode) {
        /*
         * Read-support (also covers GetNext requests)
         */
    case MODE_GET:
        for (request=requests; request; request=request->next) {

            loopCount++;
            if (request->processed != 0) /*Processed all requests, rows and colmns */
                {
                g_prevS1apGenCountersReqCount = 0;
                g_sihS1apGenCountersGetReqProcessed = FALSE;
                /* free the resources */
                free_s1apGenCountersTable();
                /* Here loopCount is decremented, Due to calling of free function two times in SNMP-GETNEXT
                 * on single parameter -- 20/07/11 */
                loopCount--;
                continue;
                }

            /* perform anything here that you need to do before each
               request is processed. */

            /* the following extracts the my_data_context pointer set in
               the loop functions above.  You can then use the results to
               help return data for the columns of the traceConfigTable table
               in question */

            table_entry = (struct s1apGenCountersTable_entry *)
                              netsnmp_extract_iterator_context(request);

            if ( table_entry == NULL) {
                if (reqinfo->mode == MODE_GET) {
                    netsnmp_set_request_error(reqinfo, request,SNMP_NOSUCHINSTANCE);
                    continue;
                }
                /* XXX: no row existed, if you support creation and this is a
                   set, start dealing with it here, else continue */
            }

            table_info  =     netsnmp_extract_table_info(      request);

            /* table_info->colnum contains the column number requested */
            /* table_info->indexes contains a linked list of snmp variable
               bindings for the indexes of the table.  Values in the list
               have been set corresponding to the indexes of the
               request */
            if (table_info==NULL) {
                continue;
            }
    
            switch (table_info->colnum) {
            case COLUMN_S1AP_GEN_COUNTERS_INDEX:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_UNSIGNED,
                                            table_entry->index);
                break;
            case COLUMN_NMBCONN:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->nmbConn);
                break;
            case COLUMN_NMBFAILCONN:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->nmbFailConn);
                break;
            case COLUMN_MAXMEMALLOC:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_UNSIGNED,
                                            table_entry->maxMemAlloc);
                break;
            case COLUMN_NMBDECERR:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->nmbDecErr);
                break;
            case COLUMN_NMBENCERR:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->nmbEncErr);
                break;
            case COLUMN_NMBAUDREQ:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->nmbAudReq);
                break;
            case COLUMN_NMBAUDCONN:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->nmbAudConn);
                break;
            case COLUMN_NMBAUDPEER:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->nmbAudPeer);
                break;
            case COLUMN_NMBAUDFLC:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->nmbAudFlc);
                break;
	    case COLUMN_S1AP_GEN_COUNTERS_LASTCOLLECTEDAT:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_value( request->requestvb, ASN_OCTET_STR,
                                          table_entry->lastCollectedAt,
                                          table_entry->lastCollectedAt_len);
                break;
            default:
                netsnmp_set_request_error(reqinfo, request,
                                          SNMP_NOSUCHOBJECT);
                break;
            }
        }
        if(g_s1apGenCountersFirstCall EQ 1)
        {
          g_s1apGenCountersFirstCall = 0;
          g_prevS1apGenCountersReqCount = loopCount;
         // This will occur in case of, when SNMP-GET or SNMP-GETNEXT of Single Parameter occurred.
         // In the first call itself loopCount becomes one.
         // In General Case, the loopCount will be always > 1 because every table contains index column to fetch the row
         // and remaining columns contain table attributes.
         if(loopCount EQ 1)
         {
           //Setting it to false in case of SNMP-GET request for single parameter.
           g_sihS1apGenCountersGetReqProcessed = FALSE;
           // Free all resources.
           free_moduleStatusTable();
         }
        }
        else if(loopCount > 1 || g_prevS1apGenCountersReqCount > 1)
        {
          //update g_prevReqCount with current loopCount for next call of Table_handler
          g_prevS1apGenCountersReqCount = loopCount;
          //nothing to be done because previous request contain more than one column.
        }
        else if(loopCount EQ 1)
        {
           //Setting it to false in case of SNMP-GET request for single parameter.
          g_sihS1apGenCountersGetReqProcessed = FALSE;
           // Free all resources.
          free_s1apGenCountersTable();
        }
        break;

    }
    return SNMP_ERR_NOERROR;
}
