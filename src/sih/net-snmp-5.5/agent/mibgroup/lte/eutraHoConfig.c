/*
 * Note: this file originally auto-generated by mib2c using
 *  : mib2c.iterate.conf 19302 2010-08-13 12:19:42Z dts12 $
 */

#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

#include "bs_modules.h"
#include "all_cfg.h"
#include "all_cmn.h"
#include "tlv_enc_dec.h"

#include "sih.h"
#include "syslog.h"

#include "eutraHoConfig.h"

Netsnmp_Node_Handler eutraHoConfigTable_handler;
Netsnmp_First_Data_Point  eutraHoConfigTable_get_first_data_point;
Netsnmp_Next_Data_Point   eutraHoConfigTable_get_next_data_point;

struct eutraHoConfigTable_entry  *eutraHoConfigTable_head = NULL;
static TBOOL g_sihEutraHoConfigSetReqProcessed = FALSE;
static TBOOL g_sihEutraHoConfigGetReqProcessed = FALSE;
static UINT32 g_prevEutraHoConfigReqCount = 0;
static UINT32 g_eutraHoConfigFirstCall = 0;

/* free_eutraHoConfigTable */
/**
* @details 
*          This is the routine which frees the eutraHo config entry. 
*/
void free_eutraHoConfigTable(void)
{
  eutraHoConfigTableEntry  * current = eutraHoConfigTable_head;

  if (current) 
  {
  for (current = eutraHoConfigTable_head->next ; current != NULL; current = current->next) {
    SNMP_FREE( current );  
    }
   
  SNMP_FREE(eutraHoConfigTable_head);
  eutraHoConfigTable_head = NULL;
  }
  return;
}


/**
* @details 
*          This is the routine which handles get response from SIH Get Processor
*          and prepare the SNMP GET response for EMS. 
* @param tlvArray contains pointer to TLV array.     
*/
TBOOL sendEutraHoConfigSnmpGetResponse (
  IO TlvArray    *tlvArray
  )
{
  int  totalNumRow, totalNumCol, i=0, colmn=0;
  eutraHoConfigTableEntry * prevPtr = NULL;

  printTLV(tlvArray);

  /* Total no. of Colmns */
  totalNumCol = EUTRAHO_CONFIG_MAX_COL;
  /* Total no. of Row = (No. of TLV)/(No. of Columns) */
  totalNumRow = tlvArray->numTlvs/totalNumCol;

  for (i=1;  i <= totalNumRow;  i++) 
      {
      eutraHoConfigTableEntry *ptr=(eutraHoConfigTableEntry *)malloc(sizeof(eutraHoConfigTableEntry));

      clearBytes(ptr,sizeof(eutraHoConfigTableEntry));

      /* Fill Data in TLV */
      for(colmn = 1 ; colmn <= totalNumCol ; colmn++)
        {
       if (colmn EQ COLUMN_HOPROFILEID)
           {
            ptr->hoProfileId = getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }        
       if (colmn EQ COLUMN_A3OFFSET)
           {
            ptr->a3Offset = getUint8FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }        
       if (colmn EQ COLUMN_REPORTONLEAVE)
           {
            ptr->reportOnLeave = getUint8FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }        
        if (colmn EQ COLUMN_A5THRESHOLD1)
           {
            ptr->a5Threshold1 = getUint8FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }
        if (colmn EQ COLUMN_A5THRESHOLD2)
           {
            ptr->a5Threshold2 = getUint8FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }
        if (colmn EQ COLUMN_HYSTERESIS)
           {
            ptr->hysteresis = getUint8FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }
        if (colmn EQ COLUMN_TIMETOTRIGGER)
           {
            ptr->timeToTrigger = getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }        
        if (colmn EQ COLUMN_TRIGGERQUANTITY)
           {
            ptr->triggerQuantity = getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }
        if (colmn EQ COLUMN_REPORTQUANTITY)
           {
            ptr->reportQuantity = getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }
        if (colmn EQ COLUMN_FILTERCOEFFICIENTRSRQ)
           {
            ptr->filterCoefficientRsrq = getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }
        if (colmn EQ COLUMN_FILTERCOEFFICIENTRSRP)
           {
            ptr->filterCoefficientRsrp = getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }
        if (colmn EQ COLUMN_REPORTINTERVAL)
           {
            ptr->reportInterval = getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }        
        if (colmn EQ COLUMN_REPORTAMOUNT)
           {
            ptr->reportAmount = getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }
        if (colmn EQ COLUMN_MAXREPORTCELLS)
           {
            ptr->maxReportCells = getUint8FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }
        if (colmn EQ COLUMN_SUPPORTEDEVENTS)
           {
            ptr->supportedEvents = getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }
        if (colmn EQ COLUMN_SMEASURE)
           {
            ptr->sMeasure = getUint8FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }
        }

       ptr->next = NULL;

      if (prevPtr == NULL) {
           eutraHoConfigTable_head =  prevPtr = ptr;
       }

      prevPtr->next = ptr;
      prevPtr = ptr;
     }
    prevPtr->next = NULL;

    if (eutraHoConfigTable_head)
        return TRUE;
    else
        return FALSE;
}

/**
* @details 
*          This routine sends eutraHo config set request to BSM, it uses SIH Set 
*          Processor to create and send the message to BSM. 
* @param sihTagInfo is used to have the tag information for set request.
*/
TBOOL sendEutraHoConfigSnmpSetRequest ( 
      IO TagInfo  sihTagInfo,
      IO TlvArray *tlvArray
      )
{
   eutraHoConfigTableEntry * setReqData = NULL;   
   TBOOL status = FALSE;
   int colmn = 0;
   unsigned char * ipAddr;
   struct in_addr ip;
 
   setReqData = eutraHoConfigTable_head;

   /* Fill Data in TLV */
   for(colmn = 0; colmn < tlvArray->numTlvs; colmn++)
      {
         if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_HOPROFILEID)
         {
           putUint32InArray( setReqData->hoProfileId, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->hoProfileId);     
         }
         if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_A3OFFSET)
         {
           putUint8InArray( setReqData->a3Offset, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->a3Offset);
         }
         if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_REPORTONLEAVE)
         {
           putUint8InArray( setReqData->reportOnLeave, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->reportOnLeave); 
         }
         if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_A5THRESHOLD1)
         {
           putUint8InArray( setReqData->a5Threshold1, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->a5Threshold1); 
         }
         if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_A5THRESHOLD2)
         {
           putUint8InArray( setReqData->a5Threshold2, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->a5Threshold2);     
         }          
         if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_HYSTERESIS)
         {
           putUint8InArray( setReqData->hysteresis, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->hysteresis);     
         }          
         if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_TIMETOTRIGGER)
         {
           putUint32InArray( setReqData->timeToTrigger, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->timeToTrigger);
         }       
         if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_TRIGGERQUANTITY)
         {
           putUint32InArray( setReqData->triggerQuantity, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->triggerQuantity);     
         }   
         if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_REPORTQUANTITY)
         {
           putUint32InArray( setReqData->reportQuantity, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->reportQuantity);     
         }          
         if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_FILTERCOEFFICIENTRSRQ)
         {
           putUint32InArray( setReqData->filterCoefficientRsrq, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->filterCoefficientRsrq);     
         }          
         if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_FILTERCOEFFICIENTRSRP)
         {
           putUint32InArray( setReqData->filterCoefficientRsrp, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->filterCoefficientRsrp);     
         }          
         if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_REPORTINTERVAL)
         {
           putUint32InArray( setReqData->reportInterval, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->reportInterval);     
         }          
         if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_REPORTAMOUNT)
         {
           putUint32InArray( setReqData->reportAmount, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->reportAmount);     
         }
         if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_MAXREPORTCELLS)
         {
           putUint8InArray( setReqData->maxReportCells, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->maxReportCells);     
         }      
         if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_SUPPORTEDEVENTS)
         {
           putUint32InArray( setReqData->supportedEvents, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->supportedEvents);     
         }
         if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_SMEASURE)
         {
           putUint8InArray( setReqData->sMeasure, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->sMeasure);     
         }
       }

   /* Invoke SIH Set Processor */
   status = sihSetProcessor (sihTagInfo,  tlvArray);
   if (status EQ TRUE)
     {
      g_sihEutraHoConfigSetReqProcessed = TRUE;
      g_sihEutraHoConfigGetReqProcessed = FALSE;
      /* free the resources */      
      free_eutraHoConfigTable(); 
     }
   return status;
}


/**
* @details 
*          This is the routine for initializing eutraHoConfig module.
*/
void
init_eutraHoConfig(void)
{
  /* here we initialize all the tables we're planning on supporting */
    initialize_table_eutraHoConfigTable();
}

/*  # Determine the first/last column names

 Initialize the eutraHoConfigTable table by defining its contents and how it's structured */
void
initialize_table_eutraHoConfigTable(void)
{
    const oid eutraHoConfigTable_oid[] = {1,3,6,1,4,1,9344,501,EUTRAHOPROFILECONFIG,1};
    const size_t eutraHoConfigTable_oid_len   = OID_LENGTH(eutraHoConfigTable_oid);
    netsnmp_handler_registration    *reg;
    netsnmp_iterator_info           *iinfo;
    netsnmp_table_registration_info *table_info;

    DEBUGMSGTL(("eutraHoConfig:init", "initializing table eutraHoConfigTable\n"));

    reg = netsnmp_create_handler_registration(
              "eutraHoConfigTable",     eutraHoConfigTable_handler,
              eutraHoConfigTable_oid, eutraHoConfigTable_oid_len,
              HANDLER_CAN_RWRITE
              );

    table_info = SNMP_MALLOC_TYPEDEF( netsnmp_table_registration_info );
    netsnmp_table_helper_add_indexes(table_info,
                           ASN_UNSIGNED,  /* index: hoProfileId */
                           0);
    table_info->min_column = COLUMN_HOPROFILEID;
    table_info->max_column = COLUMN_SMEASURE;
    
    iinfo = SNMP_MALLOC_TYPEDEF( netsnmp_iterator_info );
    iinfo->get_first_data_point = eutraHoConfigTable_get_first_data_point;
    iinfo->get_next_data_point  = eutraHoConfigTable_get_next_data_point;
    iinfo->table_reginfo        = table_info;
    
    netsnmp_register_table_iterator( reg, iinfo );

    /* Initialise the contents of the table here */
}


#ifndef PWAV_SIH 
/* create a new row in the (unsorted) table */
struct eutraHoConfigTable_entry *
eutraHoConfigTable_createEntry(
                 unsigned int  hoProfileId,
                ) {
    struct eutraHoConfigTable_entry *entry;

    entry = SNMP_MALLOC_TYPEDEF(struct eutraHoConfigTable_entry);
    if (!entry)
        return NULL;

    entry->hoProfileId = hoProfileId;
    entry->next = eutraHoConfigTable_head;
    eutraHoConfigTable_head = entry;
    return entry;
}

/* remove a row from the table */
void
eutraHoConfigTable_removeEntry( struct eutraHoConfigTable_entry *entry ) {
    struct eutraHoConfigTable_entry *ptr, *prev;

    if (!entry)
        return;    /* Nothing to remove */

    for ( ptr  = eutraHoConfigTable_head, prev = NULL;
          ptr != NULL;
          prev = ptr, ptr = ptr->next ) {
        if ( ptr == entry )
            break;
    }
    if ( !ptr )
        return;    /* Can't find it */

    if ( prev == NULL )
        eutraHoConfigTable_head = ptr->next;
    else
        prev->next = ptr->next;

    SNMP_FREE( entry );   /* XXX - release any other internal resources */
}
#endif /* PWAV_SIH */


/* Example iterator hook routines - using 'get_next' to do most of the work */
netsnmp_variable_list *
eutraHoConfigTable_get_first_data_point(void **my_loop_context,
                          void **my_data_context,
                          netsnmp_variable_list *put_index_data,
                          netsnmp_iterator_info *mydata)
{
    TagInfo sihTagInfo;
    TBOOL status = FALSE;

    if (g_sihEutraHoConfigGetReqProcessed EQ FALSE)
        {
        /* Set SIH Get Request Tag Info */
        sihTagInfo.tableId = EUTRAHOPROFILECONFIG;
        sihTagInfo.rowId = CONFIG_ROW_ID_NONE; 
        sihTagInfo.colId = CONFIG_COL_ID_NONE;                

        status = sihGetProcessor (sihTagInfo);
        if ( status EQ TRUE )
            {
            logInfo("%s","Get Processor Succedded");
            if (eutraHoConfigTable_head)
                {
		g_eutraHoConfigFirstCall = 1;
                g_sihEutraHoConfigGetReqProcessed = TRUE;
                }
            }
        else
            logError("Get Processor Failed",NULL);
        }        
    
    *my_loop_context = eutraHoConfigTable_head;
    return eutraHoConfigTable_get_next_data_point(my_loop_context, my_data_context,
                                    put_index_data,  mydata );
}

netsnmp_variable_list *
eutraHoConfigTable_get_next_data_point(void **my_loop_context,
                          void **my_data_context,
                          netsnmp_variable_list *put_index_data,
                          netsnmp_iterator_info *mydata)
{
    struct eutraHoConfigTable_entry *entry = (struct eutraHoConfigTable_entry *)*my_loop_context;
    netsnmp_variable_list *idx = put_index_data;

    if ( entry ) {
        snmp_set_var_typed_integer( idx, ASN_UNSIGNED, entry->hoProfileId );
        idx = idx->next_variable;
        *my_data_context = (void *)entry;
        *my_loop_context = (void *)entry->next;
        return put_index_data;
    } else {
        return NULL;
    }
}


/** handles requests for the eutraHoConfigTable table */
int
eutraHoConfigTable_handler(
    netsnmp_mib_handler               *handler,
    netsnmp_handler_registration      *reginfo,
    netsnmp_agent_request_info        *reqinfo,
    netsnmp_request_info              *requests) {

    netsnmp_request_info       *request = NULL;
    netsnmp_table_request_info *table_info = NULL;
    struct eutraHoConfigTable_entry          *table_entry = NULL;
    int loopCount = 0;
    int ret;
    TagInfo sihTagInfo;
    TlvArray tlvArray;
    int rowIndex = 0, colId = 0;
    TBOOL status = FALSE;    

    DEBUGMSGTL(("eutraHoConfig:handler", "Processing request (%d)\n", reqinfo->mode));

    switch (reqinfo->mode) {
        /*
         * Read-support (also covers GetNext requests)
         */
    case MODE_GET:
        for (request=requests; request; request=request->next) {

            loopCount++;
            if (request->processed != 0)
            {
	     g_prevEutraHoConfigReqCount = 0;	
             g_sihEutraHoConfigGetReqProcessed = FALSE;

             /* free the resources */
             free_eutraHoConfigTable();
             /* Here loopCount is decremented, Due to calling of free function two times in SNMP-GETNEXT
              * on single parameter -- 20/07/11 */
             loopCount--;
             continue;
            }

            /* perform anything here that you need to do before each
               request is processed. */           

            /* the following extracts the my_data_context pointer set in
               the loop functions above.  You can then use the results to
               help return data for the columns of the eutraHoConfigTable table 
               in question */    

            table_entry = (struct eutraHoConfigTable_entry *)
                              netsnmp_extract_iterator_context(request);

            
            if ( table_entry == NULL) {
                if (reqinfo->mode == MODE_GET) {
                    netsnmp_set_request_error(reqinfo, request,SNMP_NOSUCHINSTANCE);
                    continue;
                }
                /* XXX: no row existed, if you support creation and this is a
                   set, start dealing with it here, else continue */
            }

            table_info  =     netsnmp_extract_table_info(request);
    
            /* table_info->colnum contains the column number requested */
            /* table_info->indexes contains a linked list of snmp variable
               bindings for the indexes of the table.  Values in the list
               have been set corresponding to the indexes of the
               request */
            if (table_info==NULL) {
                continue;
            }        

            switch (table_info->colnum) {
            case COLUMN_HOPROFILEID:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_UNSIGNED,
                                            table_entry->hoProfileId);
                break;
            case COLUMN_A3OFFSET:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->a3Offset);
                break;
            case COLUMN_REPORTONLEAVE:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->reportOnLeave);
                break;
            case COLUMN_A5THRESHOLD1:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_UNSIGNED,
                                            table_entry->a5Threshold1);
                break;
            case COLUMN_A5THRESHOLD2:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_UNSIGNED,
                                            table_entry->a5Threshold2);
                break;
            case COLUMN_HYSTERESIS:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_UNSIGNED,
                                            table_entry->hysteresis);
                break;
            case COLUMN_TIMETOTRIGGER:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->timeToTrigger);
                break;
            case COLUMN_TRIGGERQUANTITY:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->triggerQuantity);
                break;
            case COLUMN_REPORTQUANTITY:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->reportQuantity);
                break;
            case COLUMN_FILTERCOEFFICIENTRSRQ:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->filterCoefficientRsrq);
                break;
            case COLUMN_FILTERCOEFFICIENTRSRP:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->filterCoefficientRsrp);
                break;
            case COLUMN_REPORTINTERVAL:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->reportInterval);
                break;
            case COLUMN_REPORTAMOUNT:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->reportAmount);
                break;
            case COLUMN_MAXREPORTCELLS:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_UNSIGNED,
                                            table_entry->maxReportCells);
                break;
            case COLUMN_SUPPORTEDEVENTS:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->supportedEvents);
                break;
            case COLUMN_SMEASURE:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->sMeasure);
		break;
            default:
                netsnmp_set_request_error(reqinfo, request,
                                          SNMP_NOSUCHOBJECT);
                break;
            }
        }
	if(g_eutraHoConfigFirstCall EQ 1)
         {
            g_eutraHoConfigFirstCall = 0;
            g_prevEutraHoConfigReqCount = loopCount;
            // This will occur in case of, when SNMP-GET or SNMP-GETNEXT of Single Parameter occurred.
            // In the first call itself loopCount becomes one.
            // In General Case, the loopCount will be always > 1 because every table contains 
            // index column to fetch the row and remaining columns contain table attributes.
            if(loopCount EQ 1)
            {
               //Setting it to false in case of SNMP-GET request for single parameter.
               g_sihEutraHoConfigGetReqProcessed = FALSE;
               // Free all resources.
               free_eutraHoConfigTable();
            }
         }
         else if(loopCount > 1 || g_prevEutraHoConfigReqCount > 1)
         {
            //update g_prevReqCount with current loopCount for next call of Table_handler
            g_prevEutraHoConfigReqCount = loopCount;
         }
         else if(loopCount EQ 1)
         {
            //Setting it to false in case of SNMP-GET request for single parameter.
            g_sihEutraHoConfigGetReqProcessed = FALSE;
            // Free all resources.
            free_eutraHoConfigTable();
         }
        break;

        /*
         * Write-support
         */
    case MODE_SET_RESERVE1:
#ifndef PWAV_SIH  
        for (request=requests; request; request=request->next) {
            table_entry = (struct eutraHoConfigTable_entry *)
                              netsnmp_extract_iterator_context(request);
            table_info  =     netsnmp_extract_table_info(      request);
    
            switch (table_info->colnum) {
            case COLUMN_HOPROFILEID:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_A3OFFSET:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_REPORTONLEAVE:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_A5THRESHOLD1:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_A5THRESHOLD2:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_HYSTERESIS:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_TIMETOTRIGGER:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_TRIGGERQUANTITY:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_REPORTQUANTITY:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_FILTERCOEFFICIENTRSRQ:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_FILTERCOEFFICIENTRSRP:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_REPORTINTERVAL:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_REPORTAMOUNT:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_MAXREPORTCELLS:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            default:
                netsnmp_set_request_error( reqinfo, request,
                                           SNMP_ERR_NOTWRITABLE );
                return SNMP_ERR_NOERROR;
            }
        }
#endif /* PWAV_SIH */      
        break;

    case MODE_SET_RESERVE2:
        break;

    case MODE_SET_FREE:
        break;

    case MODE_SET_ACTION:
        g_sihEutraHoConfigSetReqProcessed = FALSE; 

        clearBytes(&sihTagInfo, sizeof(TagInfo));
        clearBytes(&tlvArray, sizeof(TlvArray));

        for (request=requests; request; request=request->next) {
            table_entry = (struct eutraHoConfigTable_entry *)
                              netsnmp_extract_iterator_context(request);
            table_info  =     netsnmp_extract_table_info(      request);

            if(table_entry == NULL){
                logInfo(" %s\n", "table_entry is NULL");
                if (reqinfo->mode == MODE_SET_ACTION) {
                    netsnmp_set_request_error(reqinfo, request,SNMP_NOSUCHINSTANCE);
                    continue;
                }
                /* XXX: no row existed, if you support creation and this is a
                   set, start dealing with it here, else continue */
            }

            if(table_info == NULL){
                logInfo(" %s\n", "table_info is NULL");
                continue;
            }
    
            colId = table_info->colnum;
            /* set the row index */
            rowIndex = requests->index;

            switch (table_info->colnum) {
            case COLUMN_HOPROFILEID:
                table_entry->hoProfileId     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, EUTRAHOPROFILECONFIG, rowIndex, COLUMN_HOPROFILEID);
                tlvArray.numTlvs++;
                break;
            case COLUMN_A3OFFSET:
                table_entry->a3Offset     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, EUTRAHOPROFILECONFIG, rowIndex, COLUMN_A3OFFSET);
                tlvArray.numTlvs++;
                break;
            case COLUMN_REPORTONLEAVE:
                table_entry->reportOnLeave     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, EUTRAHOPROFILECONFIG, rowIndex, COLUMN_REPORTONLEAVE);
                tlvArray.numTlvs++;
            break;
            case COLUMN_A5THRESHOLD1:
                table_entry->a5Threshold1     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, EUTRAHOPROFILECONFIG, rowIndex, COLUMN_A5THRESHOLD1);
                tlvArray.numTlvs++;
                break;
            case COLUMN_A5THRESHOLD2:
                table_entry->a5Threshold2     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, EUTRAHOPROFILECONFIG, rowIndex, COLUMN_A5THRESHOLD2);
                tlvArray.numTlvs++;
                break;
            case COLUMN_HYSTERESIS:
                table_entry->hysteresis     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, EUTRAHOPROFILECONFIG, rowIndex, COLUMN_HYSTERESIS);
                tlvArray.numTlvs++;
                break;
            case COLUMN_TIMETOTRIGGER:
                table_entry->timeToTrigger     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, EUTRAHOPROFILECONFIG, rowIndex, COLUMN_TIMETOTRIGGER);
                tlvArray.numTlvs++;
                break;
            case COLUMN_TRIGGERQUANTITY:
                table_entry->triggerQuantity     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, EUTRAHOPROFILECONFIG, rowIndex, COLUMN_TRIGGERQUANTITY);
                tlvArray.numTlvs++;
                break;
            case COLUMN_REPORTQUANTITY:
                table_entry->reportQuantity     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, EUTRAHOPROFILECONFIG, rowIndex, COLUMN_REPORTQUANTITY);
                tlvArray.numTlvs++;
                break;
            case COLUMN_FILTERCOEFFICIENTRSRQ:
                table_entry->filterCoefficientRsrq     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, EUTRAHOPROFILECONFIG, rowIndex, COLUMN_FILTERCOEFFICIENTRSRQ);
                tlvArray.numTlvs++;
                break;
            case COLUMN_FILTERCOEFFICIENTRSRP:
                table_entry->filterCoefficientRsrp     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, EUTRAHOPROFILECONFIG, rowIndex, COLUMN_FILTERCOEFFICIENTRSRP);
                tlvArray.numTlvs++;
                break;
            case COLUMN_REPORTINTERVAL:
                table_entry->reportInterval     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, EUTRAHOPROFILECONFIG, rowIndex, COLUMN_REPORTINTERVAL);
                tlvArray.numTlvs++;
                break;
            case COLUMN_REPORTAMOUNT:
                table_entry->reportAmount     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, EUTRAHOPROFILECONFIG, rowIndex, COLUMN_REPORTAMOUNT);
                tlvArray.numTlvs++;
                break;
            case COLUMN_MAXREPORTCELLS:
                table_entry->maxReportCells     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, EUTRAHOPROFILECONFIG, rowIndex, COLUMN_MAXREPORTCELLS);
                tlvArray.numTlvs++;
                break;
            case COLUMN_SUPPORTEDEVENTS:
                table_entry->supportedEvents     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, EUTRAHOPROFILECONFIG, rowIndex, COLUMN_SUPPORTEDEVENTS);
                tlvArray.numTlvs++;
                break;
            case COLUMN_SMEASURE:
                table_entry->sMeasure     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, EUTRAHOPROFILECONFIG, rowIndex, COLUMN_SMEASURE);
                tlvArray.numTlvs++;
                break;
            }
        }

        if ((request == NULL) && (g_sihEutraHoConfigSetReqProcessed EQ FALSE))
            /* Processed all requests and table entry is filled with
             * requested data. */
            {
            /* Set SIH Set Request Tag Info */
            sihTagInfo.tableId = EUTRAHOPROFILECONFIG;

            sihTagInfo.rowId = rowIndex; 

            status = sendEutraHoConfigSnmpSetRequest (sihTagInfo, &tlvArray);
            if (status EQ TRUE)
                {
                logInfo("%s","SET Succedded");
                }
            else
                netsnmp_set_request_error(reqinfo, requests, SNMP_ERR_GENERR );
            }
        break;

    case MODE_SET_UNDO:
#ifndef PWAV_SIH 
        for (request=requests; request; request=request->next) {
            table_entry = (struct eutraHoConfigTable_entry *)
                              netsnmp_extract_iterator_context(request);
            table_info  =     netsnmp_extract_table_info(      request);
    
            switch (table_info->colnum) {
            case COLUMN_HOPROFILEID:
                table_entry->hoProfileId     = table_entry->old_hoProfileId;
                table_entry->old_hoProfileId = 0;
                break;
            case COLUMN_A3OFFSET:
                table_entry->a3Offset     = table_entry->old_a3Offset;
                table_entry->old_a3Offset = 0;
                break;
            case COLUMN_REPORTONLEAVE:
                table_entry->reportOnLeave     = table_entry->old_reportOnLeave;
                table_entry->old_reportOnLeave = 0;
                break;
            case COLUMN_A5THRESHOLD1:
                table_entry->a5Threshold1     = table_entry->old_a5Threshold1;
                table_entry->old_a5Threshold1 = 0;
                break;
            case COLUMN_A5THRESHOLD2:
                table_entry->a5Threshold2     = table_entry->old_a5Threshold2;
                table_entry->old_a5Threshold2 = 0;
                break;
            case COLUMN_HYSTERESIS:
                table_entry->hysteresis     = table_entry->old_hysteresis;
                table_entry->old_hysteresis = 0;
                break;
            case COLUMN_TIMETOTRIGGER:
                table_entry->timeToTrigger     = table_entry->old_timeToTrigger;
                table_entry->old_timeToTrigger = 0;
                break;
            case COLUMN_TRIGGERQUANTITY:
                table_entry->triggerQuantity     = table_entry->old_triggerQuantity;
                table_entry->old_triggerQuantity = 0;
                break;
            case COLUMN_REPORTQUANTITY:
                table_entry->reportQuantity     = table_entry->old_reportQuantity;
                table_entry->old_reportQuantity = 0;
                break;
            case COLUMN_FILTERCOEFFICIENTRSRQ:
                table_entry->filterCoefficientRsrq     = table_entry->old_filterCoefficientRsrq;
                table_entry->old_filterCoefficientRsrq = 0;
                break;
            case COLUMN_FILTERCOEFFICIENTRSRP:
                table_entry->filterCoefficientRsrp     = table_entry->old_filterCoefficientRsrp;
                table_entry->old_filterCoefficientRsrp = 0;
                break;
            case COLUMN_REPORTINTERVAL:
                table_entry->reportInterval     = table_entry->old_reportInterval;
                table_entry->old_reportInterval = 0;
                break;
            case COLUMN_REPORTAMOUNT:
                table_entry->reportAmount     = table_entry->old_reportAmount;
                table_entry->old_reportAmount = 0;
                break;
            case COLUMN_MAXREPORTCELLS:
                table_entry->maxReportCells     = table_entry->old_maxReportCells;
                table_entry->old_maxReportCells = 0;
                break;
            }
        }
#endif /* PWAV_SIH */
        break;

    case MODE_SET_COMMIT:
        break;
    }
    return SNMP_ERR_NOERROR;
}
