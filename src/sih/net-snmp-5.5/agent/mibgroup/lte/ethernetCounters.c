/*
 * Note: this file originally auto-generated by mib2c using
 *  : mib2c.iterate.conf 19302 2010-08-13 12:19:42Z dts12 $
 */

#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

#include "bs_modules.h"
#include "all_cfg.h"
#include "all_cmn.h"
#include "tlv_enc_dec.h"

#include "sih.h"
#include "syslog.h"
#include "ethernetCounters.h"

/* function declarations */
Netsnmp_Node_Handler ethernetCountersTable_handler;
Netsnmp_First_Data_Point  ethernetCountersTable_get_first_data_point;
Netsnmp_Next_Data_Point   ethernetCountersTable_get_next_data_point;

/* globals */
struct ethernetCountersTable_entry  *ethernetCountersTable_head = NULL;
static TBOOL g_sihEthernetCountersGetReqProcessed = FALSE;
static UINT32 g_prevEthernetCountersReqCount = 0;
static UINT32 g_ethernetCountersFirstCall = 0;

/* free_ethernetCountersTable */
/**
* @details 
*          This is the routine which frees the ethernet counters entry.
*/
void free_ethernetCountersTable(void)
{
  ethernetCountersTableEntry  * current = ethernetCountersTable_head;
  ethernetCountersTableEntry  * temp = NULL;

  if(current)
  {
    while(current->next != NULL)
    {
        temp = current->next;
        SNMP_FREE( current );
        current = temp;
    }

    SNMP_FREE( current );
    ethernetCountersTable_head = NULL;
  }
  return;
}


/**
* @details 
*          This is the routine which handles get response from SIH Get Processor
*          and prepare the SNMP GET response for EMS.
* @param tlvArray contains pointer to TLV array.
*/
TBOOL sendEthernetCountersSnmpGetResponse (
  IO TlvArray    *tlvArray
  )
{
  int  totalNumRow, totalNumCol, i=0, colmn=0, pos=0;
  ethernetCountersTableEntry * prevPtr = NULL;

  printTLV(tlvArray);

  /* Total no. of Colmns */
  totalNumCol = ETHERNET_COUNTERS_MAX_COL;
  /* Total no. of Row = (No. of TLV)/(No. of Columns) */
  totalNumRow = tlvArray->numTlvs/totalNumCol;

  logInfo("%s","Total No. of Row %d & Col %d", totalNumRow, totalNumCol);
  for (i=1;  i <= totalNumRow;  i++)
      {
      ethernetCountersTableEntry *ptr=(ethernetCountersTableEntry *)malloc(sizeof(ethernetCountersTableEntry));

      clearBytes (ptr, sizeof(ethernetCountersTableEntry));

      /* Fill Data in TLV */
      for(colmn = 1 ; colmn <= totalNumCol ; colmn++, pos++)
        {
        if (colmn EQ COLUMN_PWAV_IFINDEX)
           {
            ptr->ifIndex =  getUint32FromArray(tlvArray->tlvInfo[pos].data);
            continue;
           }
		if (colmn EQ COLUMN_PWAV_IFDESCR)
           {
            clearBytes (ptr->ifDescr, ETHERNET_COUNTERS_IFDESC_LEN_MAX);
            copyBytes (tlvArray->tlvInfo[pos].data, tlvArray->tlvInfo[pos].len, ptr->ifDescr);
            ptr->ifDescr_len = strlen(ptr->ifDescr);
            continue;
           }
		if (colmn EQ COLUMN_PWAV_IFTYPE)
           {
            ptr->ifType =  getUint32FromArray(tlvArray->tlvInfo[pos].data);
            continue;
           }
		if (colmn EQ COLUMN_PWAV_IFMTU)
           {
            ptr->ifMtu =  getUint32FromArray(tlvArray->tlvInfo[pos].data);
            continue;
           }
		if (colmn EQ COLUMN_PWAV_IFSPEED)
           {
            ptr->ifSpeed =  getUint32FromArray(tlvArray->tlvInfo[pos].data);
            continue;
           }
		if (colmn EQ COLUMN_PWAV_IFPHYSADDRESS)
           {
            clearBytes (ptr->ifPhysAddress, ETHERNET_COUNTERS_PHY_ADDR_LEN_MAX);
            copyBytes (tlvArray->tlvInfo[pos].data, tlvArray->tlvInfo[pos].len, ptr->ifPhysAddress);
            ptr->ifPhysAddress_len = strlen(ptr->ifPhysAddress);
            continue;
           }
		if (colmn EQ COLUMN_PWAV_IFADMINSTATUS)
           {
            ptr->ifAdminStatus =  getUint32FromArray(tlvArray->tlvInfo[pos].data);
            continue;
           }
		if (colmn EQ COLUMN_PWAV_IFOPERSTATUS)
           {
            ptr->ifOperStatus =  getUint32FromArray(tlvArray->tlvInfo[pos].data);
            continue;
           }
		if (colmn EQ COLUMN_PWAV_IFLASTCHANGE)
           {
            ptr->ifLastChange =  getUint32FromArray(tlvArray->tlvInfo[pos].data);
            continue;
           }
		if (colmn EQ COLUMN_PWAV_IFINOCTETS)
           {
            ptr->ifInOctets =  getUint32FromArray(tlvArray->tlvInfo[pos].data);
            continue;
           }
		if (colmn EQ COLUMN_PWAV_IFINUCASTPKTS)
           {
            ptr->ifInUcastPkts =  getUint32FromArray(tlvArray->tlvInfo[pos].data);
            continue;
           }
		if (colmn EQ COLUMN_PWAV_IFINNUCASTPKTS)
           {
            ptr->ifInNUcastPkts =  getUint32FromArray(tlvArray->tlvInfo[pos].data);
            continue;
           }
		if (colmn EQ COLUMN_PWAV_IFINDISCARDS)
           {
            ptr->ifInDiscards =  getUint32FromArray(tlvArray->tlvInfo[pos].data);
            continue;
           }
		if (colmn EQ COLUMN_PWAV_IFINERRORS)
           {
            ptr->ifInErrors =  getUint32FromArray(tlvArray->tlvInfo[pos].data);
            continue;
           }
		if (colmn EQ COLUMN_PWAV_IFINUNKNOWNPROTOS)
           {
            ptr->ifInUnknownProtos =  getUint32FromArray(tlvArray->tlvInfo[pos].data);
            continue;
           }
		if (colmn EQ COLUMN_PWAV_IFOUTOCTETS)
           {
            ptr->ifOutOctets =  getUint32FromArray(tlvArray->tlvInfo[pos].data);
            continue;
           }
		if (colmn EQ COLUMN_PWAV_IFOUTUCASTPKTS)
           {
            ptr->ifOutUcastPkts =  getUint32FromArray(tlvArray->tlvInfo[pos].data);
            continue;
           }
		if (colmn EQ COLUMN_PWAV_IFOUTNUCASTPKTS)
           {
            ptr->ifOutNUcastPkts =  getUint32FromArray(tlvArray->tlvInfo[pos].data);
            continue;
           }
		if (colmn EQ COLUMN_PWAV_IFOUTDISCARDS)
           {
            ptr->ifOutDiscards =  getUint32FromArray(tlvArray->tlvInfo[pos].data);
            continue;
           }
		if (colmn EQ COLUMN_PWAV_IFOUTERRORS)
           {
            ptr->ifOutErrors =  getUint32FromArray(tlvArray->tlvInfo[pos].data);
            continue;
           }
		if (colmn EQ COLUMN_PWAV_IFOUTQLEN)
           {
            ptr->ifOutQLen =  getUint32FromArray(tlvArray->tlvInfo[pos].data);
            continue;
           }
        if (colmn EQ COLUMN_PWAV_ETHERNET_COUNTERS_LASTCOLLECTEDAT)
           {
            clearBytes (ptr->lastCollectedAt, ETHERNET_COUNTERS_LCOLLECT_BUF_LEN_MAX);
            copyBytes (tlvArray->tlvInfo[pos].data, tlvArray->tlvInfo[pos].len, ptr->lastCollectedAt);
            ptr->lastCollectedAt_len = strlen(ptr->lastCollectedAt);
            continue;
           }
        }

       ptr->next = NULL;

      if (prevPtr == NULL) {
           ethernetCountersTable_head =  prevPtr = ptr;
       }

      prevPtr->next = ptr;
      prevPtr = ptr;
     }
    prevPtr->next = NULL;

    if (ethernetCountersTable_head)
        return TRUE;
    else
        return FALSE;
}


/** Initializes the ethernetCounters module */
void
init_ethernetCounters(void)
{
  /* here we initialize all the tables we're planning on supporting */
    initialize_table_ethernetCountersTable();
}

/* Determine the first/last column names */

/** Initialize the ethernetCountersTable table by defining its contents and how it's structured */
void
initialize_table_ethernetCountersTable(void)
{
    const oid ethernetCountersTable_oid[] = {1,3,6,1,4,1,9344,501,ETHERNETCOUNTERS,1};
    const size_t ethernetCountersTable_oid_len   = OID_LENGTH(ethernetCountersTable_oid);
    netsnmp_handler_registration    *reg;
    netsnmp_iterator_info           *iinfo;
    netsnmp_table_registration_info *table_info;

    DEBUGMSGTL(("ethernetCounters:init", "initializing table ethernetCountersTable\n"));

    reg = netsnmp_create_handler_registration(
              "ethernetCountersTable",     ethernetCountersTable_handler,
              ethernetCountersTable_oid, ethernetCountersTable_oid_len,
              HANDLER_CAN_RONLY
              );

    table_info = SNMP_MALLOC_TYPEDEF( netsnmp_table_registration_info );
    netsnmp_table_helper_add_indexes(table_info,
                           ASN_UNSIGNED,  /* index: ifIndex */
                           0);
    table_info->min_column = COLUMN_PWAV_IFINDEX;
    table_info->max_column = COLUMN_PWAV_ETHERNET_COUNTERS_LASTCOLLECTEDAT;
    
    iinfo = SNMP_MALLOC_TYPEDEF( netsnmp_iterator_info );
    iinfo->get_first_data_point = ethernetCountersTable_get_first_data_point;
    iinfo->get_next_data_point  = ethernetCountersTable_get_next_data_point;
    iinfo->table_reginfo        = table_info;
    
    netsnmp_register_table_iterator( reg, iinfo );

    /* Initialise the contents of the table here */
}


#ifndef PWAV_SIH
/* create a new row in the (unsorted) table */
struct ethernetCountersTable_entry *
ethernetCountersTable_createEntry(
                 long  ifIndex,
                ) {
    struct ethernetCountersTable_entry *entry;

    entry = SNMP_MALLOC_TYPEDEF(struct ethernetCountersTable_entry);
    if (!entry)
        return NULL;

    entry->ifIndex = ifIndex;
    entry->next = ethernetCountersTable_head;
    ethernetCountersTable_head = entry;
    return entry;
}

/* remove a row from the table */
void
ethernetCountersTable_removeEntry( struct ethernetCountersTable_entry *entry ) {
    struct ethernetCountersTable_entry *ptr, *prev;

    if (!entry)
        return;    /* Nothing to remove */

    for ( ptr  = ethernetCountersTable_head, prev = NULL;
          ptr != NULL;
          prev = ptr, ptr = ptr->next ) {
        if ( ptr == entry )
            break;
    }
    if ( !ptr )
        return;    /* Can't find it */

    if ( prev == NULL )
        ethernetCountersTable_head = ptr->next;
    else
        prev->next = ptr->next;

    SNMP_FREE( entry );   /* XXX - release any other internal resources */
}
#endif /* PWAV_SIH */


/* Example iterator hook routines - using 'get_next' to do most of the work */
netsnmp_variable_list *
ethernetCountersTable_get_first_data_point(void **my_loop_context,
                          void **my_data_context,
                          netsnmp_variable_list *put_index_data,
                          netsnmp_iterator_info *mydata)
{
    TagInfo sihTagInfo;
    TBOOL status = FALSE;


    if (g_sihEthernetCountersGetReqProcessed EQ FALSE)
        {
        /* Set SIH Get Request Tag Info */
        sihTagInfo.tableId = ETHERNETCOUNTERS;
        sihTagInfo.rowId = CONFIG_ROW_ID_NONE;
        sihTagInfo.colId = CONFIG_COL_ID_NONE;

        status = sihGetProcessor(sihTagInfo);
        if ( status EQ TRUE )
            {
            logInfo("%s","Get Processor Succedded");
            if (ethernetCountersTable_head)
                {
		g_ethernetCountersFirstCall = 1;
                g_sihEthernetCountersGetReqProcessed = TRUE;
                }
            }
        else
            logError("%s","Get Processor Failed");
        }


    *my_loop_context = ethernetCountersTable_head;
    return ethernetCountersTable_get_next_data_point(my_loop_context, my_data_context,
                                    put_index_data,  mydata );
}

netsnmp_variable_list *
ethernetCountersTable_get_next_data_point(void **my_loop_context,
                          void **my_data_context,
                          netsnmp_variable_list *put_index_data,
                          netsnmp_iterator_info *mydata)
{
    struct ethernetCountersTable_entry *entry = (struct ethernetCountersTable_entry *)*my_loop_context;
    netsnmp_variable_list *idx = put_index_data;

    if ( entry ) {
        snmp_set_var_typed_integer( idx, ASN_UNSIGNED, entry->ifIndex );
        idx = idx->next_variable;
        *my_data_context = (void *)entry;
        *my_loop_context = (void *)entry->next;
        return put_index_data;
    } else {
        return NULL;
    }
}


/** handles requests for the ethernetCountersTable table */
int
ethernetCountersTable_handler(
    netsnmp_mib_handler               *handler,
    netsnmp_handler_registration      *reginfo,
    netsnmp_agent_request_info        *reqinfo,
    netsnmp_request_info              *requests) {

    netsnmp_request_info       *request = NULL;
    netsnmp_table_request_info *table_info = NULL;
    struct ethernetCountersTable_entry          *table_entry = NULL;
    int loopCount = 0;

    DEBUGMSGTL(("ethernetCounters:handler", "Processing request (%d)\n", reqinfo->mode));

    switch (reqinfo->mode) {
        /*
         * Read-support (also covers GetNext requests)
         */
    case MODE_GET:
        for (request=requests; request; request=request->next) {

            loopCount++;
            if (request->processed != 0) /*Processed all requests, rows and colmns */
                {
		g_prevEthernetCountersReqCount = 0;
                g_sihEthernetCountersGetReqProcessed = FALSE;
                /* free the resources */
                free_ethernetCountersTable();
                /* Here loopCount is decremented, Due to calling of free function two times in SNMP-GETNEXT
                 * on single parameter -- 20/07/11 */
                loopCount--;
                continue;
                }

            /* perform anything here that you need to do before each
               request is processed. */

            /* the following extracts the my_data_context pointer set in
               the loop functions above.  You can then use the results to
               help return data for the columns of the ethernetCountersTable table
               in question */

            table_entry = (struct ethernetCountersTable_entry *)
                              netsnmp_extract_iterator_context(request);

            if ( table_entry == NULL) {
                if (reqinfo->mode == MODE_GET) {
                    netsnmp_set_request_error(reqinfo, request,SNMP_NOSUCHINSTANCE);
                    continue;
                }
                /* XXX: no row existed, if you support creation and this is a
                   set, start dealing with it here, else continue */
            }

            table_info  =     netsnmp_extract_table_info(      request);

            /* table_info->colnum contains the column number requested */
            /* table_info->indexes contains a linked list of snmp variable
               bindings for the indexes of the table.  Values in the list
               have been set corresponding to the indexes of the
               request */
            if (table_info==NULL) {
                continue;
            }
    
            switch (table_info->colnum) {
            case COLUMN_PWAV_IFINDEX:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_UNSIGNED,
                                            table_entry->ifIndex);
                break;
            case COLUMN_PWAV_IFDESCR:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_value( request->requestvb, ASN_OCTET_STR,
                                          table_entry->ifDescr,
                                          table_entry->ifDescr_len);
                break;
            case COLUMN_PWAV_IFTYPE:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_UNSIGNED,
                                            table_entry->ifType);
                break;
            case COLUMN_PWAV_IFMTU:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->ifMtu);
                break;
            case COLUMN_PWAV_IFSPEED:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_GAUGE,
                                            table_entry->ifSpeed);
                break;
            case COLUMN_PWAV_IFPHYSADDRESS:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_value( request->requestvb, ASN_OCTET_STR,
                                          table_entry->ifPhysAddress,
                                          table_entry->ifPhysAddress_len);
                break;
            case COLUMN_PWAV_IFADMINSTATUS:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_UNSIGNED,
                                            table_entry->ifAdminStatus);
                break;
            case COLUMN_PWAV_IFOPERSTATUS:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_UNSIGNED,
                                            table_entry->ifOperStatus);
                break;
            case COLUMN_PWAV_IFLASTCHANGE:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_TIMETICKS,
                                            table_entry->ifLastChange);
                break;
            case COLUMN_PWAV_IFINOCTETS:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_COUNTER,
                                            table_entry->ifInOctets);
                break;
            case COLUMN_PWAV_IFINUCASTPKTS:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_COUNTER,
                                            table_entry->ifInUcastPkts);
                break;
            case COLUMN_PWAV_IFINNUCASTPKTS:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_COUNTER,
                                            table_entry->ifInNUcastPkts);
                break;
            case COLUMN_PWAV_IFINDISCARDS:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_COUNTER,
                                            table_entry->ifInDiscards);
                break;
            case COLUMN_PWAV_IFINERRORS:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_COUNTER,
                                            table_entry->ifInErrors);
                break;
            case COLUMN_PWAV_IFINUNKNOWNPROTOS:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_COUNTER,
                                            table_entry->ifInUnknownProtos);
                break;
            case COLUMN_PWAV_IFOUTOCTETS:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_COUNTER,
                                            table_entry->ifOutOctets);
                break;
            case COLUMN_PWAV_IFOUTUCASTPKTS:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_COUNTER,
                                            table_entry->ifOutUcastPkts);
                break;
            case COLUMN_PWAV_IFOUTNUCASTPKTS:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_COUNTER,
                                            table_entry->ifOutNUcastPkts);
                break;
            case COLUMN_PWAV_IFOUTDISCARDS:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_COUNTER,
                                            table_entry->ifOutDiscards);
                break;
            case COLUMN_PWAV_IFOUTERRORS:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_COUNTER,
                                            table_entry->ifOutErrors);
                break;
            case COLUMN_PWAV_IFOUTQLEN:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_GAUGE,
                                            table_entry->ifOutQLen);
                break;
	     case COLUMN_PWAV_ETHERNET_COUNTERS_LASTCOLLECTEDAT:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_value( request->requestvb, ASN_OCTET_STR,
                                          table_entry->lastCollectedAt,
                                          table_entry->lastCollectedAt_len);
                break;
            default:
                netsnmp_set_request_error(reqinfo, request,
                                          SNMP_NOSUCHOBJECT);
                break;
            }
        }
	if(g_ethernetCountersFirstCall EQ 1)
         {
            g_ethernetCountersFirstCall = 0;
            g_prevEthernetCountersReqCount = loopCount;
            // This will occur in case of, when SNMP-GET or SNMP-GETNEXT of Single Parameter occurred.
            // In the first call itself loopCount becomes one.
            // In General Case, the loopCount will be always > 1 because every table contains 
            // index column to fetch the row and remaining columns contain table attributes.
            if(loopCount EQ 1)
            {
               //Setting it to false in case of SNMP-GET request for single parameter.
               g_sihEthernetCountersGetReqProcessed = FALSE;
               // Free all resources.
               free_ethernetCountersTable();
            }
         }
         else if(loopCount > 1 || g_prevEthernetCountersReqCount > 1)
         {
            //update g_prevReqCount with current loopCount for next call of Table_handler
            g_prevEthernetCountersReqCount = loopCount;
         }
         else if(loopCount EQ 1)
         {
            //Setting it to false in case of SNMP-GET request for single parameter.
            g_sihEthernetCountersGetReqProcessed = FALSE;
            // Free all resources.
            free_ethernetCountersTable();
         }
        break;

    }
    return SNMP_ERR_NOERROR;
}
