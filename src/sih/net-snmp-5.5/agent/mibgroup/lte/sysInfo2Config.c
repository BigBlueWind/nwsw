/*
 * Note: this file originally auto-generated by mib2c using
 *  : mib2c.iterate.conf 17695 2009-07-21 12:22:18Z dts12 $
 */

#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

#include "bs_modules.h"
#include "all_cfg.h"
#include "all_cmn.h"
#include "tlv_enc_dec.h"

#include "sih.h"
#include "syslog.h"

#include "sysInfo2Config.h"

/* function declarations */
Netsnmp_Node_Handler sysInfo2ConfigTable_handler;
Netsnmp_First_Data_Point  sysInfo2ConfigTable_get_first_data_point;
Netsnmp_Next_Data_Point   sysInfo2ConfigTable_get_next_data_point;


/* globals */
struct sysInfo2ConfigTable_entry  *sysInfo2ConfigTable_head = NULL;
static TBOOL g_sihSysInfo2ConfigSetReqProcessed = FALSE;
static TBOOL g_sihSysInfo2ConfigGetReqProcessed = FALSE;
static UINT32 g_prevSysInfo2ConfigReqCount = 0;
static UINT32 g_sysInfo2ConfigFirstCall = 0;

/* free_sysInfo2ConfigTable */
/**
* @details 
*          This is the routine which frees the bs config entry. 
*/
void free_sysInfo2ConfigTable(void)
{
  sysInfo2ConfigTableEntry  * current = sysInfo2ConfigTable_head;

  if (current) 
  {
  for (current = sysInfo2ConfigTable_head->next ; current != NULL; current = current->next) {
    SNMP_FREE( current );  
    }
   
  SNMP_FREE(sysInfo2ConfigTable_head);
  sysInfo2ConfigTable_head = NULL;
  }
  return;
}

/**
* @details 
*          This is the routine which handles get response from SIH Get Processor
*          and prepare the SNMP GET response for EMS. 
* @param tlvArray contains pointer to TLV array.     
*/
TBOOL sendSysInfo2ConfigSnmpGetResponse (
  IO TlvArray    *tlvArray
  )
{
  int  totalNumRow, totalNumCol, i=0, colmn=0;
  sysInfo2ConfigTableEntry * prevPtr = NULL;

  printTLV(tlvArray);

  /* Total no. of Colmns */
  totalNumCol = SYS_INFO2_CONFIG_MAX_COL;

  /* Total no. of Row = (No. of TLV)/(No. of Columns) */
  totalNumRow = tlvArray->numTlvs/totalNumCol;

  for (i=1;  i <= totalNumRow;  i++) 
      {
      sysInfo2ConfigTableEntry *ptr=(sysInfo2ConfigTableEntry *)malloc(sizeof(sysInfo2ConfigTableEntry));

      clearBytes(ptr,sizeof(sysInfo2ConfigTableEntry));

      /* Fill Data in TLV */
      for(colmn = 1 ; colmn <= totalNumCol ; colmn++)
        {
       if (colmn EQ COLUMN_SYSINFO2PROFILEID)
           {
            ptr->sysInfo2ProfileId = getUint8FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }        
       if (colmn EQ COLUMN_NUMRAPREAMBLES)
           {
            ptr->numRaPreambles = getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }        
       if (colmn EQ COLUMN_SIZERAPREAMBLESGROUPA)
           {
            ptr->sizeRAPreamblesGroupA = getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }        
       if (colmn EQ COLUMN_MESSAGESIZEGROUPA)
           {
            ptr->messageSizeGroupA = getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }        
       if (colmn EQ COLUMN_MESSAGEPOWEROFFSETGROUPB)
           {
            ptr->messagePowerOffsetGroupB = getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }        
       if (colmn EQ COLUMN_POWERRAMPINGSTEP)
           {
            ptr->powerRampingStep = getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }        
       if (colmn EQ COLUMN_PREAMBLEINITRCVDTGTPOWER)
           {
            ptr->preambleInitRcvdTgtPower = getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }        
       if (colmn EQ COLUMN_PREAMBLETRANSMAX)
           {
            ptr->preambleTransMax = getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }        
       if (colmn EQ COLUMN_RARSPWINDOWSIZE)
           {
            ptr->raRspWindowSize = getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }        
       if (colmn EQ COLUMN_MACCONTRESOLUTIONTIMER)
           {
            ptr->macContResolutionTimer = getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }        
       if (colmn EQ COLUMN_MAXHARQMSG3TX)
           {
            ptr->maxHarqMsg3Tx = getUint8FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }        
       if (colmn EQ COLUMN_MODIFICATIONPERIODCOEFF)
           {
            ptr->modificationPeriodCoeff = getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }        
       if (colmn EQ COLUMN_DEFAULTPAGCYCLE)
           {
            ptr->defaultPagCycle = getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }        
       if (colmn EQ COLUMN_NB)
           {
            ptr->nB = getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }        
       if (colmn EQ COLUMN_ROOTSEQUENCEINDEX)
           {
            ptr->rootSequenceIndex = getUint16FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }        
       if (colmn EQ COLUMN_PRACHCONFIGINDEX)
           {
            ptr->prachConfigIndex = getUint8FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }        
       if (colmn EQ COLUMN_HIGHSPEEDFLAG)
           {
            ptr->highSpeedFlag = getUint8FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }               
       if (colmn EQ COLUMN_ZEROCORRELATIONZONECONFIG)
           {
            ptr->zeroCorrelationZoneConfig = getUint8FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }               
       if (colmn EQ COLUMN_PUSCHFREQHOPPINGSUBBANDS)
           {
            ptr->puschFreqHoppingSubBands = getUint8FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }               
       if (colmn EQ COLUMN_PUSCHFREQHOPPINGMODE)
           {
            ptr->puschFreqHoppingMode = getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }               
       if (colmn EQ COLUMN_PUSCHFREQHOPPINGOFFSET)
           {
            ptr->puschFreqHoppingOffset = getUint8FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }               
       if (colmn EQ COLUMN_ULRSGROUPHOPPINGENABLED)
           {
            ptr->ulRsGroupHoppingEnabled = getUint8FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }               
       if (colmn EQ COLUMN_ULRSGROUPPUSCH)
           {
            ptr->ulRsGroupPusch = getUint8FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }               
       if (colmn EQ COLUMN_ULRSSEQHOPPINGENABLED)
           {
            ptr->ulRsSeqHoppingEnabled = getUint8FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }               
       if (colmn EQ COLUMN_ULRSCYCLICSHIFTPUSCH)
           {
            ptr->ulRsCyclicShiftPusch = getUint8FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }               
       if (colmn EQ COLUMN_DELTAPUCCHSHIFT)
           {
            ptr->deltaPucchShift = getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }               
       if (colmn EQ COLUMN_N1PUCCHAN)
           {
            ptr->n1PucchAn = getUint16FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }               
       if (colmn EQ COLUMN_SRSBANDWIDTHCONFIG)
           {
            ptr->srsBandwidthConfig = getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }               
       if (colmn EQ COLUMN_SRSSUBFRMCONFIG)
           {
            ptr->srsSubfrmConfig = getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }               
       if (colmn EQ COLUMN_SRSSIMULTANEOUSANSRSTX)
           {
            ptr->srsSimultaneousAnSrsTx = getUint8FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }               
       if (colmn EQ COLUMN_PONOMINALPUSCH)
           {
            ptr->poNominalPusch = getUint8FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }               
       if (colmn EQ COLUMN_DELTAFPUCCHFORMAT1)
           {
            ptr->deltaFPucchFormat1 = getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }               
       if (colmn EQ COLUMN_DELTAFPUCCHFORMAT1B)
           {
            ptr->deltaFPucchFormat1b = getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }               
       if (colmn EQ COLUMN_DELTAFPUCCHFORMAT2)
           {
            ptr->deltaFPucchFormat2 = getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }               
       if (colmn EQ COLUMN_DELTAFPUCCHFORMAT2A)
           {
            ptr->deltaFPucchFormat2a = getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }               
       if (colmn EQ COLUMN_DELTAFPUCCHFORMAT2B)
           {
            ptr->deltaFPucchFormat2b = getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }               
       if (colmn EQ COLUMN_DELTAPREAMBLEMSG3)
           {
            ptr->deltaPreambleMsg3 = getUint8FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }               
       if (colmn EQ COLUMN_T300)
           {
            ptr->t300 = getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }               
       if (colmn EQ COLUMN_T301)
           {
            ptr->t301 = getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }               
       if (colmn EQ COLUMN_T310)
           {
            ptr->t310 = getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }               
       if (colmn EQ COLUMN_N310)
           {
            ptr->n310 = getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }               
       if (colmn EQ COLUMN_T311)
           {
            ptr->t311 = getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }               
       if (colmn EQ COLUMN_N311)
           {
            ptr->n311 = getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }               
       if (colmn EQ COLUMN_ADDITIONALSPECTRUMEMISSION)
           {
            ptr->additionalSpectrumEmission = getUint8FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }               
       if (colmn EQ COLUMN_TIMEALIGNMENTTIMER)
           {
            ptr->timeAlignmentTimer = getUint32FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }               
       if (colmn EQ COLUMN_NRBCQI)
           {
            ptr->nRbCqi = getUint8FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }
       if (colmn EQ COLUMN_ENABLE64QAM)
           {
            ptr->enable64QAM = getUint8FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }
       if (colmn EQ COLUMN_NCSAN)
           {
            ptr->nCsAn = getUint8FromArray(tlvArray->tlvInfo[colmn-1].data);
            continue;
           }
        }

       ptr->next = NULL;

      if (prevPtr == NULL) {
           sysInfo2ConfigTable_head =  prevPtr = ptr;
       }

      prevPtr->next = ptr;
      prevPtr = ptr;
     }
    prevPtr->next = NULL;

    if (sysInfo2ConfigTable_head)
        return TRUE;
    else
        return FALSE;
}


/**
* @details 
*          This routine sends bs config set request to BSM, it uses SIH Set 
*          Processor to create and send the message to BSM. 
* @param sihTagInfo is used to have the tag information for set request.
*/
TBOOL sendSysInfo2ConfigSnmpSetRequest ( 
      IO TagInfo  sihTagInfo,
      IO TlvArray *tlvArray
      )
{
   sysInfo2ConfigTableEntry * setReqData = NULL;   
   TBOOL status = FALSE;
   int colmn = 0, maxCols = 0;
 
   setReqData = sysInfo2ConfigTable_head;

   /* Fill Data in TLV */
   for(colmn = 0; colmn < tlvArray->numTlvs; colmn++)
   {
       if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_SYSINFO2PROFILEID)
       {
           putUint8InArray( setReqData->sysInfo2ProfileId, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->sysInfo2ProfileId);
       }
       if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_NUMRAPREAMBLES)
       {
           putUint32InArray( setReqData->numRaPreambles, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->numRaPreambles);
       }       
       if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_SIZERAPREAMBLESGROUPA)
       {
           putUint32InArray( setReqData->sizeRAPreamblesGroupA, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->sizeRAPreamblesGroupA);     
       }       
       if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_MESSAGESIZEGROUPA)
       {
           putUint32InArray( setReqData->messageSizeGroupA, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->messageSizeGroupA); 
       }       
       if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_MESSAGEPOWEROFFSETGROUPB)
       {
           putUint32InArray( setReqData->messagePowerOffsetGroupB, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->messagePowerOffsetGroupB); 
       }       
       if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_POWERRAMPINGSTEP)
       {
           putUint32InArray( setReqData->powerRampingStep, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->powerRampingStep); 
       }       
       if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_PREAMBLEINITRCVDTGTPOWER)
       {
           putUint32InArray( setReqData->preambleInitRcvdTgtPower, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->preambleInitRcvdTgtPower); 
       }       
       if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_PREAMBLETRANSMAX)
       {
           putUint32InArray( setReqData->preambleTransMax, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->preambleTransMax); 
       }       
       if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_RARSPWINDOWSIZE)
       {
           putUint32InArray( setReqData->raRspWindowSize, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->raRspWindowSize); 
       }       
       if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_MACCONTRESOLUTIONTIMER)
       {
           putUint32InArray( setReqData->macContResolutionTimer, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->macContResolutionTimer); 
       }       
       if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_MAXHARQMSG3TX)
       {
           putUint8InArray( setReqData->maxHarqMsg3Tx, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->maxHarqMsg3Tx); 
       }       
       if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_MODIFICATIONPERIODCOEFF)
       {
           putUint32InArray( setReqData->modificationPeriodCoeff, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->modificationPeriodCoeff); 
       }       
       if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_DEFAULTPAGCYCLE)
       {
           putUint32InArray( setReqData->defaultPagCycle, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->defaultPagCycle); 
       }       
       if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_NB)
       {
           putUint32InArray( setReqData->nB, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->nB); 
       }       
       if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_ROOTSEQUENCEINDEX)
       {
           putUint16InArray( setReqData->rootSequenceIndex, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->rootSequenceIndex); 
       }       
       if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_PRACHCONFIGINDEX)
       {
           putUint8InArray( setReqData->prachConfigIndex, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->prachConfigIndex); 
       }      
       if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_HIGHSPEEDFLAG)
       {
           putUint8InArray( setReqData->highSpeedFlag, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->highSpeedFlag); 
       }       
       if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_ZEROCORRELATIONZONECONFIG)
       {
           putUint8InArray( setReqData->zeroCorrelationZoneConfig, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->zeroCorrelationZoneConfig); 
       }       
       if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_PUSCHFREQHOPPINGSUBBANDS)
       {
           putUint8InArray( setReqData->puschFreqHoppingSubBands, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->puschFreqHoppingSubBands); 
       }       
       if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_PUSCHFREQHOPPINGMODE)
       {
           putUint32InArray( setReqData->puschFreqHoppingMode, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->puschFreqHoppingMode); 
       }       
       if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_PUSCHFREQHOPPINGOFFSET)
       {
           putUint8InArray( setReqData->puschFreqHoppingOffset, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->puschFreqHoppingOffset); 
       }       
       if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_ULRSGROUPHOPPINGENABLED)
       {
           putUint8InArray( setReqData->ulRsGroupHoppingEnabled, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->ulRsGroupHoppingEnabled); 
       }       
       if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_ULRSGROUPPUSCH)
       {
           putUint8InArray( setReqData->ulRsGroupPusch, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->ulRsGroupPusch); 
       }       
       if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_ULRSSEQHOPPINGENABLED)
       {
           putUint32InArray( setReqData->ulRsSeqHoppingEnabled, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->ulRsSeqHoppingEnabled); 
       }       
       if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_ULRSCYCLICSHIFTPUSCH)
       {
           putUint8InArray( setReqData->ulRsCyclicShiftPusch, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->ulRsCyclicShiftPusch); 
       }       
       if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_DELTAPUCCHSHIFT)
       {
           putUint32InArray( setReqData->deltaPucchShift, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->deltaPucchShift); 
       }       
       if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_N1PUCCHAN)
       {
           putUint16InArray( setReqData->n1PucchAn, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->n1PucchAn); 
       }       
       if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_SRSBANDWIDTHCONFIG)
       {
           putUint32InArray( setReqData->srsBandwidthConfig, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->srsBandwidthConfig); 
       }       
       if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_SRSSUBFRMCONFIG)
       {
           putUint32InArray( setReqData->srsSubfrmConfig, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->srsSubfrmConfig); 
       }       
       if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_SRSSIMULTANEOUSANSRSTX)
       {
           putUint8InArray( setReqData->srsSimultaneousAnSrsTx, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->srsSimultaneousAnSrsTx); 
       }       
       if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_PONOMINALPUSCH)
       {
           putUint8InArray( setReqData->poNominalPusch, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->poNominalPusch); 
       }       
       if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_DELTAFPUCCHFORMAT1)
       {
           putUint32InArray( setReqData->deltaFPucchFormat1, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->deltaFPucchFormat1); 
       }       
       if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_DELTAFPUCCHFORMAT1B)
       {
           putUint32InArray( setReqData->deltaFPucchFormat1b, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->deltaFPucchFormat1b); 
       }       
       if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_DELTAFPUCCHFORMAT2)
       {
           putUint32InArray( setReqData->deltaFPucchFormat2, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->deltaFPucchFormat2); 
       }       
       if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_DELTAFPUCCHFORMAT2A)
       {
           putUint32InArray( setReqData->deltaFPucchFormat2a, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->deltaFPucchFormat2a); 
       }       
       if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_DELTAFPUCCHFORMAT2B)
       {
           putUint32InArray( setReqData->deltaFPucchFormat2b, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->deltaFPucchFormat2b); 
       }       
       if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_DELTAPREAMBLEMSG3)
       {
           putUint8InArray( setReqData->deltaPreambleMsg3, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->deltaPreambleMsg3); 
       }       
       if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_T300)
       {
           putUint32InArray( setReqData->t300, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->t300); 
       }       
       if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_T301)
       {
           putUint32InArray( setReqData->t301, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->t301); 
       }       
       if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_T310)
       {
           putUint32InArray( setReqData->t310, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->t310); 
       }       
       if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_N310)
       {
           putUint32InArray( setReqData->n310, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->n310); 
       }       
       if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_T311)
       {
           putUint32InArray( setReqData->t311, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->t311); 
       }       
       if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_N311)
       {
           putUint32InArray( setReqData->n311, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->n311); 
       }       
       if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_ADDITIONALSPECTRUMEMISSION)
       {
           putUint8InArray( setReqData->additionalSpectrumEmission, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->additionalSpectrumEmission); 
       }       
       if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_TIMEALIGNMENTTIMER)
       {
           putUint32InArray( setReqData->timeAlignmentTimer, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->timeAlignmentTimer); 
       }       
       if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_NRBCQI)
       {
           putUint8InArray( setReqData->nRbCqi, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->nRbCqi); 
       }       
       if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_ENABLE64QAM)
       {
           putUint8InArray( setReqData->enable64QAM, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->enable64QAM); 
       }       
       if(tlvArray->tlvInfo[colmn].tag.colId EQ COLUMN_NCSAN)
       {
           putUint8InArray( setReqData->nCsAn, tlvArray->tlvInfo[colmn].data );
           tlvArray->tlvInfo[colmn].len = sizeof(setReqData->nCsAn); 
       }
   }

   /* Invoke SIH Set Processor */
   status = sihSetProcessor (sihTagInfo, tlvArray);
   if (status EQ TRUE)
     {
      g_sihSysInfo2ConfigSetReqProcessed = TRUE;
      g_sihSysInfo2ConfigGetReqProcessed = FALSE;
      /* free the resources */      
      free_sysInfo2ConfigTable(); 
     }
   return status;
}



/** Initializes the sysInfo2Config module */
void
init_sysInfo2Config(void)
{
  /* here we initialize all the tables we're planning on supporting */
    initialize_table_sysInfo2ConfigTable();
}

/* # Determine the first/last column names */

/** Initialize the sysInfo2ConfigTable table by defining its contents and how it's structured */
void
initialize_table_sysInfo2ConfigTable(void)
{
    const oid sysInfo2ConfigTable_oid[] = {1,3,6,1,4,1,9344,501,SYSINFO2CONFIG,1};
    const size_t sysInfo2ConfigTable_oid_len   = OID_LENGTH(sysInfo2ConfigTable_oid);
    netsnmp_handler_registration    *reg;
    netsnmp_iterator_info           *iinfo;
    netsnmp_table_registration_info *table_info;

    DEBUGMSGTL(("sysInfo2Config:init", "initializing table sysInfo2ConfigTable\n"));

    reg = netsnmp_create_handler_registration(
              "sysInfo2ConfigTable",     sysInfo2ConfigTable_handler,
              sysInfo2ConfigTable_oid, sysInfo2ConfigTable_oid_len,
              HANDLER_CAN_RWRITE
              );

    table_info = SNMP_MALLOC_TYPEDEF( netsnmp_table_registration_info );
    netsnmp_table_helper_add_indexes(table_info,
                           ASN_UNSIGNED,  /* index: sysInfo2ProfileId */
                           0);
    table_info->min_column = COLUMN_SYSINFO2PROFILEID;
    table_info->max_column = SYS_INFO2_CONFIG_MAX_COL;
    
    iinfo = SNMP_MALLOC_TYPEDEF( netsnmp_iterator_info );
    iinfo->get_first_data_point = sysInfo2ConfigTable_get_first_data_point;
    iinfo->get_next_data_point  = sysInfo2ConfigTable_get_next_data_point;
    iinfo->table_reginfo        = table_info;
    
    netsnmp_register_table_iterator( reg, iinfo );

    /* Initialise the contents of the table here */
}


#ifndef PWAV_SIH
/* create a new row in the (unsorted) table */
struct sysInfo2ConfigTable_entry *
sysInfo2ConfigTable_createEntry(
                 u_long  sysInfo2ProfileId,
                ) {
    struct sysInfo2ConfigTable_entry *entry;

    entry = SNMP_MALLOC_TYPEDEF(struct sysInfo2ConfigTable_entry);
    if (!entry)
        return NULL;

    entry->sysInfo2ProfileId = sysInfo2ProfileId;
    entry->next = sysInfo2ConfigTable_head;
    sysInfo2ConfigTable_head = entry;
    return entry;
}

/* remove a row from the table */
void
sysInfo2ConfigTable_removeEntry( struct sysInfo2ConfigTable_entry *entry ) {
    struct sysInfo2ConfigTable_entry *ptr, *prev;

    if (!entry)
        return;    /* Nothing to remove */

    for ( ptr  = sysInfo2ConfigTable_head, prev = NULL;
          ptr != NULL;
          prev = ptr, ptr = ptr->next ) {
        if ( ptr == entry )
            break;
    }
    if ( !ptr )
        return;    /* Can't find it */

    if ( prev == NULL )
        sysInfo2ConfigTable_head = ptr->next;
    else
        prev->next = ptr->next;

    SNMP_FREE( entry );   /* XXX - release any other internal resources */
}
#endif /* PWAV_SIH */


/* Example iterator hook routines - using 'get_next' to do most of the work */
netsnmp_variable_list *
sysInfo2ConfigTable_get_first_data_point(void **my_loop_context,
                          void **my_data_context,
                          netsnmp_variable_list *put_index_data,
                          netsnmp_iterator_info *mydata)
{
    TagInfo sihTagInfo;
    TBOOL status = FALSE;

    if (g_sihSysInfo2ConfigGetReqProcessed EQ FALSE)
        {
        /* Set SIH Get Request Tag Info */
        sihTagInfo.tableId = SYSINFO2CONFIG;
        sihTagInfo.rowId = CONFIG_ROW_ID_NONE; 
        sihTagInfo.colId = CONFIG_COL_ID_NONE;                

        status = sihGetProcessor (sihTagInfo);
        if ( status EQ TRUE )
            {
            logInfo("%s","Get Processor Succedded");
            if (sysInfo2ConfigTable_head)
                {
		g_sysInfo2ConfigFirstCall = 1;
                g_sihSysInfo2ConfigGetReqProcessed = TRUE;
                }
            }
        else
            logError("%s","Get Processor Failed");
        }     

    *my_loop_context = sysInfo2ConfigTable_head;
    return sysInfo2ConfigTable_get_next_data_point(my_loop_context, my_data_context,
                                    put_index_data,  mydata );
}

netsnmp_variable_list *
sysInfo2ConfigTable_get_next_data_point(void **my_loop_context,
                          void **my_data_context,
                          netsnmp_variable_list *put_index_data,
                          netsnmp_iterator_info *mydata)
{
    struct sysInfo2ConfigTable_entry *entry = (struct sysInfo2ConfigTable_entry *)*my_loop_context;
    netsnmp_variable_list *idx = put_index_data;

    if ( entry ) {
        snmp_set_var_typed_integer( idx, ASN_UNSIGNED, entry->sysInfo2ProfileId );
        idx = idx->next_variable;
        *my_data_context = (void *)entry;
        *my_loop_context = (void *)entry->next;
        return put_index_data;
    } else {
        return NULL;
    }
}


/** handles requests for the sysInfo2ConfigTable table */
int
sysInfo2ConfigTable_handler(
    netsnmp_mib_handler               *handler,
    netsnmp_handler_registration      *reginfo,
    netsnmp_agent_request_info        *reqinfo,
    netsnmp_request_info              *requests) {

    netsnmp_request_info       *request = NULL;
    netsnmp_table_request_info *table_info = NULL;
    struct sysInfo2ConfigTable_entry          *table_entry = NULL;
    int loopCount = 0;
    int ret;
    TagInfo sihTagInfo;
    TlvArray tlvArray;
    int rowIndex = 0, colId = 0;
    TBOOL status = FALSE;   

    DEBUGMSGTL(("sysInfo2Config:handler", "Processing request (%d)\n", reqinfo->mode));

    switch (reqinfo->mode) {
        /*
         * Read-support (also covers GetNext requests)
         */
    case MODE_GET:

        for (request=requests; request; request=request->next) {

            loopCount++;
            if (request->processed != 0)
              {
	       g_prevSysInfo2ConfigReqCount = 0;	
               /*Processed all requests, rows and colmns */
               g_sihSysInfo2ConfigGetReqProcessed = FALSE;
               /* free the resources */
               free_sysInfo2ConfigTable(); 
               /* Here loopCount is decremented, Due to calling of free function two times in SNMP-GETNEXT
                * on single parameter -- 20/07/11 */
               loopCount--;
               continue;
              }


            /* perform anything here that you need to do before each
               request is processed. */           

            /* the following extracts the my_data_context pointer set in
               the loop functions above.  You can then use the results to
               help return data for the columns of the sysInfo1ConfigTable table 
               in question */   

            table_entry = (struct sysInfo2ConfigTable_entry *)
                              netsnmp_extract_iterator_context(request);

            if ( table_entry == NULL) {
                if (reqinfo->mode == MODE_GET) {
                    netsnmp_set_request_error(reqinfo, request,SNMP_NOSUCHINSTANCE);
                    continue;
                }
                /* XXX: no row existed, if you support creation and this is a
                   set, start dealing with it here, else continue */
            }

            table_info  =     netsnmp_extract_table_info(      request);
            /* table_info->colnum contains the column number requested */
            /* table_info->indexes contains a linked list of snmp variable
               bindings for the indexes of the table.  Values in the list
               have been set corresponding to the indexes of the
               request */
            if (table_info==NULL) {
                continue;
            }  
    
            switch (table_info->colnum) {
            case COLUMN_SYSINFO2PROFILEID:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_UNSIGNED,
                                            table_entry->sysInfo2ProfileId);
                break;
            case COLUMN_NUMRAPREAMBLES:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->numRaPreambles);
                break;
            case COLUMN_SIZERAPREAMBLESGROUPA:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->sizeRAPreamblesGroupA);
                break;
            case COLUMN_MESSAGESIZEGROUPA:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->messageSizeGroupA);
                break;
            case COLUMN_MESSAGEPOWEROFFSETGROUPB:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->messagePowerOffsetGroupB);
                break;
            case COLUMN_POWERRAMPINGSTEP:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->powerRampingStep);
                break;
            case COLUMN_PREAMBLEINITRCVDTGTPOWER:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->preambleInitRcvdTgtPower);
                break;
            case COLUMN_PREAMBLETRANSMAX:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->preambleTransMax);
                break;
            case COLUMN_RARSPWINDOWSIZE:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->raRspWindowSize);
                break;
            case COLUMN_MACCONTRESOLUTIONTIMER:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->macContResolutionTimer);
                break;
            case COLUMN_MAXHARQMSG3TX:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_UNSIGNED,
                                            table_entry->maxHarqMsg3Tx);
                break;
            case COLUMN_MODIFICATIONPERIODCOEFF:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->modificationPeriodCoeff);
                break;
            case COLUMN_DEFAULTPAGCYCLE:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->defaultPagCycle);
                break;
            case COLUMN_NB:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->nB);
                break;
            case COLUMN_ROOTSEQUENCEINDEX:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_UNSIGNED,
                                            table_entry->rootSequenceIndex);
                break;
            case COLUMN_PRACHCONFIGINDEX:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_UNSIGNED,
                                            table_entry->prachConfigIndex);
                break;
            case COLUMN_HIGHSPEEDFLAG:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->highSpeedFlag);
                break;
            case COLUMN_ZEROCORRELATIONZONECONFIG:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_UNSIGNED,
                                            table_entry->zeroCorrelationZoneConfig);
                break;
            case COLUMN_PUSCHFREQHOPPINGSUBBANDS:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_UNSIGNED,
                                            table_entry->puschFreqHoppingSubBands);
                break;
            case COLUMN_PUSCHFREQHOPPINGMODE:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->puschFreqHoppingMode);
                break;
            case COLUMN_PUSCHFREQHOPPINGOFFSET:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_UNSIGNED,
                                            table_entry->puschFreqHoppingOffset);
                break;
            case COLUMN_ULRSGROUPHOPPINGENABLED:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_UNSIGNED,
                                            table_entry->ulRsGroupHoppingEnabled);
                break;
            case COLUMN_ULRSGROUPPUSCH:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_UNSIGNED,
                                            table_entry->ulRsGroupPusch);
                break;
            case COLUMN_ULRSSEQHOPPINGENABLED:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->ulRsSeqHoppingEnabled);
                break;
            case COLUMN_ULRSCYCLICSHIFTPUSCH:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_UNSIGNED,
                                            table_entry->ulRsCyclicShiftPusch);
                break;
            case COLUMN_DELTAPUCCHSHIFT:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->deltaPucchShift);
                break;
            case COLUMN_N1PUCCHAN:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_UNSIGNED,
                                            table_entry->n1PucchAn);
                break;
            case COLUMN_SRSBANDWIDTHCONFIG:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->srsBandwidthConfig);
                break;
            case COLUMN_SRSSUBFRMCONFIG:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->srsSubfrmConfig);
                break;
            case COLUMN_SRSSIMULTANEOUSANSRSTX:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->srsSimultaneousAnSrsTx);
                break;
            case COLUMN_PONOMINALPUSCH:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->poNominalPusch);
                break;
            case COLUMN_DELTAFPUCCHFORMAT1:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->deltaFPucchFormat1);
                break;
            case COLUMN_DELTAFPUCCHFORMAT1B:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->deltaFPucchFormat1b);
                break;
            case COLUMN_DELTAFPUCCHFORMAT2:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->deltaFPucchFormat2);
                break;
            case COLUMN_DELTAFPUCCHFORMAT2A:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->deltaFPucchFormat2a);
                break;
            case COLUMN_DELTAFPUCCHFORMAT2B:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->deltaFPucchFormat2b);
                break;
            case COLUMN_DELTAPREAMBLEMSG3:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->deltaPreambleMsg3);
                break;
            case COLUMN_T300:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->t300);
                break;
            case COLUMN_T301:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->t301);
                break;
            case COLUMN_T310:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->t310);
                break;
            case COLUMN_N310:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->n310);
                break;
            case COLUMN_T311:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->t311);
                break;
            case COLUMN_N311:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->n311);
                break;
            case COLUMN_ADDITIONALSPECTRUMEMISSION:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_UNSIGNED,
                                            table_entry->additionalSpectrumEmission);
                break;
            case COLUMN_TIMEALIGNMENTTIMER:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_INTEGER,
                                            table_entry->timeAlignmentTimer);
                break;
            case COLUMN_NRBCQI:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_UNSIGNED,
                                            table_entry->nRbCqi);
                break;
            case COLUMN_ENABLE64QAM:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_UNSIGNED,
                                            table_entry->enable64QAM);
                break;
            case COLUMN_NCSAN:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_UNSIGNED,
                                            table_entry->nCsAn);
                break;
            default:
                netsnmp_set_request_error(reqinfo, request,
                                          SNMP_NOSUCHOBJECT);
                break;
            }
        }
	if(g_sysInfo2ConfigFirstCall EQ 1)
         {
            g_sysInfo2ConfigFirstCall = 0;
            g_prevSysInfo2ConfigReqCount = loopCount;
            // This will occur in case of, when SNMP-GET or SNMP-GETNEXT of Single Parameter occurred.
            // In the first call itself loopCount becomes one.
            // In General Case, the loopCount will be always > 1 because every table contains 
            // index column to fetch the row and remaining columns contain table attributes.
            if(loopCount EQ 1)
            {
               //Setting it to false in case of SNMP-GET request for single parameter.
               g_sihSysInfo2ConfigGetReqProcessed = FALSE;
               // Free all resources.
               free_sysInfo2ConfigTable();
            }
         }
         else if(loopCount > 1 || g_prevSysInfo2ConfigReqCount > 1)
         {
            //update g_prevReqCount with current loopCount for next call of Table_handler
            g_prevSysInfo2ConfigReqCount = loopCount;
         }
         else if(loopCount EQ 1)
         {
            //Setting it to false in case of SNMP-GET request for single parameter.
            g_sihSysInfo2ConfigGetReqProcessed = FALSE;
            // Free all resources.
            free_sysInfo2ConfigTable();
         }
        break;

        /*
         * Write-support
         */
    case MODE_SET_RESERVE1:
#ifndef PWAV_SIH
        for (request=requests; request; request=request->next) {
            table_entry = (struct sysInfo2ConfigTable_entry *)
                              netsnmp_extract_iterator_context(request);
            table_info  =     netsnmp_extract_table_info(      request);
    
            switch (table_info->colnum) {
            case COLUMN_SYSINFO2PROFILEID:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_NUMRAPREAMBLES:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_SIZERAPREAMBLESGROUPA:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_MESSAGESIZEGROUPA:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_MESSAGEPOWEROFFSETGROUPB:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_POWERRAMPINGSTEP:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_PREAMBLEINITRCVDTGTPOWER:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_PREAMBLETRANSMAX:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_RARSPWINDOWSIZE:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_MACCONTRESOLUTIONTIMER:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_MAXHARQMSG3TX:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_MODIFICATIONPERIODCOEFF:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_DEFAULTPAGCYCLE:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_NB:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_ROOTSEQUENCEINDEX:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_PRACHCONFIGINDEX:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_HIGHSPEEDFLAG:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_ZEROCORRELATIONZONECONFIG:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_PUSCHFREQHOPPINGSUBBANDS:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_PUSCHFREQHOPPINGMODE:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_PUSCHFREQHOPPINGOFFSET:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_ULRSGROUPHOPPINGENABLED:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_ULRSGROUPPUSCH:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_ULRSSEQHOPPINGENABLED:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_ULRSCYCLICSHIFTPUSCH:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_DELTAPUCCHSHIFT:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_N1PUCCHAN:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_SRSBANDWIDTHCONFIG:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_SRSSUBFRMCONFIG:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_SRSSIMULTANEOUSANSRSTX:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_PONOMINALPUSCH:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_DELTAFPUCCHFORMAT1:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_DELTAFPUCCHFORMAT1B:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_DELTAFPUCCHFORMAT2:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_DELTAFPUCCHFORMAT2A:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_DELTAFPUCCHFORMAT2B:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_DELTAPREAMBLEMSG3:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_T300:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_T301:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_T310:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_N310:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_T311:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_N311:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_ADDITIONALSPECTRUMEMISSION:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_TIMEALIGNMENTTIMER:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_NRBCQI:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_ENABLE64QAM:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_NCSAN:
                /* or possibly 'netsnmp_check_vb_int_range' */
                ret = netsnmp_check_vb_int( request->requestvb );
                if ( ret != SNMP_ERR_NOERROR ) {
                    netsnmp_set_request_error( reqinfo, request, ret );
                    return SNMP_ERR_NOERROR;
                }
                break;
            default:
                netsnmp_set_request_error( reqinfo, request,
                                           SNMP_ERR_NOTWRITABLE );
                return SNMP_ERR_NOERROR;
            }
        }
#endif /* PWAV_SIH */
        break;

    case MODE_SET_RESERVE2:
        break;

    case MODE_SET_FREE:
        break;

    case MODE_SET_ACTION:
	g_sihSysInfo2ConfigSetReqProcessed = FALSE;

        clearBytes(&sihTagInfo, sizeof(TagInfo));
        clearBytes(&tlvArray, sizeof(TlvArray));

        for (request=requests; request; request=request->next) {
            table_entry = (struct sysInfo2ConfigTable_entry *)
                              netsnmp_extract_iterator_context(request);
            table_info  =     netsnmp_extract_table_info(request);

            if(table_entry == NULL){
                logInfo(" %s\n", "table_entry is NULL");
                if (reqinfo->mode == MODE_SET_ACTION) {
                    netsnmp_set_request_error(reqinfo, request,SNMP_NOSUCHINSTANCE);
                    continue;
                }
                /* XXX: no row existed, if you support creation and this is a
                   set, start dealing with it here, else continue */
            }

            if(table_info == NULL){
                logInfo(" %s\n", "table_info is NULL");
                continue;
            }
   
            colId = table_info->colnum;
            /* set the row index */
            rowIndex = requests->index;
 
            switch (table_info->colnum) {
            case COLUMN_SYSINFO2PROFILEID:
                table_entry->sysInfo2ProfileId     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, SYSINFO2CONFIG, rowIndex, COLUMN_SYSINFO2PROFILEID);
                tlvArray.numTlvs++;
                break;
            case COLUMN_NUMRAPREAMBLES:
                table_entry->numRaPreambles     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, SYSINFO2CONFIG, rowIndex, COLUMN_NUMRAPREAMBLES);
                tlvArray.numTlvs++;
                break;
            case COLUMN_SIZERAPREAMBLESGROUPA:
                table_entry->sizeRAPreamblesGroupA     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, SYSINFO2CONFIG, rowIndex, COLUMN_SIZERAPREAMBLESGROUPA);
                tlvArray.numTlvs++;
                break;
            case COLUMN_MESSAGESIZEGROUPA:
                table_entry->messageSizeGroupA     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, SYSINFO2CONFIG, rowIndex, COLUMN_MESSAGESIZEGROUPA);
                tlvArray.numTlvs++;
                break;
            case COLUMN_MESSAGEPOWEROFFSETGROUPB:
                table_entry->messagePowerOffsetGroupB     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, SYSINFO2CONFIG, rowIndex, COLUMN_MESSAGEPOWEROFFSETGROUPB);
                tlvArray.numTlvs++;
                break;
            case COLUMN_POWERRAMPINGSTEP:
                table_entry->powerRampingStep     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, SYSINFO2CONFIG, rowIndex, COLUMN_POWERRAMPINGSTEP);
                tlvArray.numTlvs++;
                break;
            case COLUMN_PREAMBLEINITRCVDTGTPOWER:
                table_entry->preambleInitRcvdTgtPower     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, SYSINFO2CONFIG, rowIndex, COLUMN_PREAMBLEINITRCVDTGTPOWER);
                tlvArray.numTlvs++;
                break;
            case COLUMN_PREAMBLETRANSMAX:
                table_entry->preambleTransMax     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, SYSINFO2CONFIG, rowIndex, COLUMN_PREAMBLETRANSMAX);
                tlvArray.numTlvs++;
                break;
            case COLUMN_RARSPWINDOWSIZE:
                table_entry->raRspWindowSize     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, SYSINFO2CONFIG, rowIndex, COLUMN_RARSPWINDOWSIZE);
                tlvArray.numTlvs++;
                break;
            case COLUMN_MACCONTRESOLUTIONTIMER:
                table_entry->macContResolutionTimer     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, SYSINFO2CONFIG, rowIndex, COLUMN_MACCONTRESOLUTIONTIMER);
                tlvArray.numTlvs++;
                break;
            case COLUMN_MAXHARQMSG3TX:
                table_entry->maxHarqMsg3Tx     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, SYSINFO2CONFIG, rowIndex, COLUMN_MAXHARQMSG3TX);
                tlvArray.numTlvs++;
                break;
            case COLUMN_MODIFICATIONPERIODCOEFF:
                table_entry->modificationPeriodCoeff     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, SYSINFO2CONFIG, rowIndex, COLUMN_MODIFICATIONPERIODCOEFF);
                tlvArray.numTlvs++;
                break;
            case COLUMN_DEFAULTPAGCYCLE:
                table_entry->defaultPagCycle     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, SYSINFO2CONFIG, rowIndex, COLUMN_DEFAULTPAGCYCLE);
                tlvArray.numTlvs++;
                break;
            case COLUMN_NB:
                table_entry->nB     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, SYSINFO2CONFIG, rowIndex, COLUMN_NB);
                tlvArray.numTlvs++;
                break;
            case COLUMN_ROOTSEQUENCEINDEX:
                table_entry->rootSequenceIndex     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, SYSINFO2CONFIG, rowIndex, COLUMN_ROOTSEQUENCEINDEX);
                tlvArray.numTlvs++;
                break;
            case COLUMN_PRACHCONFIGINDEX:
                table_entry->prachConfigIndex     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, SYSINFO2CONFIG, rowIndex, COLUMN_PRACHCONFIGINDEX);
                tlvArray.numTlvs++;
                break;
            case COLUMN_HIGHSPEEDFLAG:
                table_entry->highSpeedFlag     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, SYSINFO2CONFIG, rowIndex, COLUMN_HIGHSPEEDFLAG);
                tlvArray.numTlvs++;
                break;
            case COLUMN_ZEROCORRELATIONZONECONFIG:
                table_entry->zeroCorrelationZoneConfig     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, SYSINFO2CONFIG, rowIndex, COLUMN_ZEROCORRELATIONZONECONFIG);
                tlvArray.numTlvs++;
                break;
            case COLUMN_PUSCHFREQHOPPINGSUBBANDS:
                table_entry->puschFreqHoppingSubBands     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, SYSINFO2CONFIG, rowIndex, COLUMN_PUSCHFREQHOPPINGSUBBANDS);
                tlvArray.numTlvs++;
                break;
            case COLUMN_PUSCHFREQHOPPINGMODE:
                table_entry->puschFreqHoppingMode     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, SYSINFO2CONFIG, rowIndex, COLUMN_PUSCHFREQHOPPINGMODE);
                tlvArray.numTlvs++;
                break;
            case COLUMN_PUSCHFREQHOPPINGOFFSET:
                table_entry->puschFreqHoppingOffset     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, SYSINFO2CONFIG, rowIndex, COLUMN_PUSCHFREQHOPPINGOFFSET);
                tlvArray.numTlvs++;
                break;
            case COLUMN_ULRSGROUPHOPPINGENABLED:
                table_entry->ulRsGroupHoppingEnabled     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, SYSINFO2CONFIG, rowIndex, COLUMN_ULRSGROUPHOPPINGENABLED);
                tlvArray.numTlvs++;
                break;
            case COLUMN_ULRSGROUPPUSCH:
                table_entry->ulRsGroupPusch     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, SYSINFO2CONFIG, rowIndex, COLUMN_ULRSGROUPPUSCH);
                tlvArray.numTlvs++;
                break;
            case COLUMN_ULRSSEQHOPPINGENABLED:
                table_entry->ulRsSeqHoppingEnabled     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, SYSINFO2CONFIG, rowIndex, COLUMN_ULRSSEQHOPPINGENABLED);
                tlvArray.numTlvs++;
                break;
            case COLUMN_ULRSCYCLICSHIFTPUSCH:
                table_entry->ulRsCyclicShiftPusch     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, SYSINFO2CONFIG, rowIndex, COLUMN_ULRSCYCLICSHIFTPUSCH);
                tlvArray.numTlvs++;
                break;
            case COLUMN_DELTAPUCCHSHIFT:
                table_entry->deltaPucchShift     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, SYSINFO2CONFIG, rowIndex, COLUMN_DELTAPUCCHSHIFT);
                tlvArray.numTlvs++;
                break;
            case COLUMN_N1PUCCHAN:
                table_entry->n1PucchAn     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, SYSINFO2CONFIG, rowIndex, COLUMN_N1PUCCHAN);
                tlvArray.numTlvs++;
                break;
            case COLUMN_SRSBANDWIDTHCONFIG:
                table_entry->srsBandwidthConfig     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, SYSINFO2CONFIG, rowIndex, COLUMN_SRSBANDWIDTHCONFIG);
                tlvArray.numTlvs++;
                break;
            case COLUMN_SRSSUBFRMCONFIG:
                table_entry->srsSubfrmConfig     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, SYSINFO2CONFIG, rowIndex, COLUMN_SRSSUBFRMCONFIG);
                tlvArray.numTlvs++;
                break;
            case COLUMN_SRSSIMULTANEOUSANSRSTX:
                table_entry->srsSimultaneousAnSrsTx     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, SYSINFO2CONFIG, rowIndex, COLUMN_SRSSIMULTANEOUSANSRSTX);
                tlvArray.numTlvs++;
                break;
            case COLUMN_PONOMINALPUSCH:
                table_entry->poNominalPusch     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, SYSINFO2CONFIG, rowIndex, COLUMN_PONOMINALPUSCH);
                tlvArray.numTlvs++;
                break;
            case COLUMN_DELTAFPUCCHFORMAT1:
                table_entry->deltaFPucchFormat1     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, SYSINFO2CONFIG, rowIndex, COLUMN_DELTAFPUCCHFORMAT1);
                tlvArray.numTlvs++;
                break;
            case COLUMN_DELTAFPUCCHFORMAT1B:
                table_entry->deltaFPucchFormat1b     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, SYSINFO2CONFIG, rowIndex, COLUMN_DELTAFPUCCHFORMAT1B);
                tlvArray.numTlvs++;
                break;
            case COLUMN_DELTAFPUCCHFORMAT2:
                table_entry->deltaFPucchFormat2     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, SYSINFO2CONFIG, rowIndex, COLUMN_DELTAFPUCCHFORMAT2);
                tlvArray.numTlvs++;
                break;
            case COLUMN_DELTAFPUCCHFORMAT2A:
                table_entry->deltaFPucchFormat2a     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, SYSINFO2CONFIG, rowIndex, COLUMN_DELTAFPUCCHFORMAT2A);
                tlvArray.numTlvs++;
                break;
            case COLUMN_DELTAFPUCCHFORMAT2B:
                table_entry->deltaFPucchFormat2b     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, SYSINFO2CONFIG, rowIndex, COLUMN_DELTAFPUCCHFORMAT2B);
                tlvArray.numTlvs++;
                break;
            case COLUMN_DELTAPREAMBLEMSG3:
                table_entry->deltaPreambleMsg3     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, SYSINFO2CONFIG, rowIndex, COLUMN_DELTAPREAMBLEMSG3);
                tlvArray.numTlvs++;
                break;
            case COLUMN_T300:
                table_entry->t300     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, SYSINFO2CONFIG, rowIndex, COLUMN_T300);
                tlvArray.numTlvs++;
                break;
            case COLUMN_T301:
                table_entry->t301     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, SYSINFO2CONFIG, rowIndex, COLUMN_T301);
                tlvArray.numTlvs++;
                break;
            case COLUMN_T310:
                table_entry->t310     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, SYSINFO2CONFIG, rowIndex, COLUMN_T310);
                tlvArray.numTlvs++;
                break;
            case COLUMN_N310:
                table_entry->n310     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, SYSINFO2CONFIG, rowIndex, COLUMN_N310);
                tlvArray.numTlvs++;
                break;
            case COLUMN_T311:
                table_entry->t311     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, SYSINFO2CONFIG, rowIndex, COLUMN_T311);
                tlvArray.numTlvs++;
                break;
            case COLUMN_N311:
                table_entry->n311     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, SYSINFO2CONFIG, rowIndex, COLUMN_N311);
                tlvArray.numTlvs++;
                break;
            case COLUMN_ADDITIONALSPECTRUMEMISSION:
                table_entry->additionalSpectrumEmission     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, SYSINFO2CONFIG, rowIndex, COLUMN_ADDITIONALSPECTRUMEMISSION);
                tlvArray.numTlvs++;
                break;
            case COLUMN_TIMEALIGNMENTTIMER:
                table_entry->timeAlignmentTimer     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, SYSINFO2CONFIG, rowIndex, COLUMN_TIMEALIGNMENTTIMER);
                tlvArray.numTlvs++;
                break;
            case COLUMN_NRBCQI:
                table_entry->nRbCqi     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, SYSINFO2CONFIG, rowIndex, COLUMN_NRBCQI);
                tlvArray.numTlvs++;
                break;
            case COLUMN_ENABLE64QAM:
                table_entry->enable64QAM     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, SYSINFO2CONFIG, rowIndex, COLUMN_ENABLE64QAM);
                tlvArray.numTlvs++;
                break;
            case COLUMN_NCSAN:
                table_entry->nCsAn     = *request->requestvb->val.integer;
                fillTableInfo(&tlvArray.tlvInfo[tlvArray.numTlvs].tag, SYSINFO2CONFIG, rowIndex, COLUMN_NCSAN);
                tlvArray.numTlvs++;
                break;
            }
        }

        if ((request == NULL) && (g_sihSysInfo2ConfigSetReqProcessed EQ FALSE))
            /* Processed all requests and table entry is filled with
             * requested data. */
            {
            /* Set SIH Set Request Tag Info */
            sihTagInfo.tableId = SYSINFO2CONFIG;

            sihTagInfo.rowId = rowIndex; 

            status = sendSysInfo2ConfigSnmpSetRequest (sihTagInfo, &tlvArray);
            if (status EQ TRUE)
                {
                logInfo("%s","SET Succedded");
                }
            else
                netsnmp_set_request_error(reqinfo, requests, SNMP_ERR_GENERR );
            }
        break;

    case MODE_SET_UNDO:
#ifndef PWAV_SIH
        for (request=requests; request; request=request->next) {
            table_entry = (struct sysInfo2ConfigTable_entry *)
                              netsnmp_extract_iterator_context(request);
            table_info  =     netsnmp_extract_table_info(      request);
    
            switch (table_info->colnum) {
            case COLUMN_SYSINFO2PROFILEID:
                table_entry->sysInfo2ProfileId     = table_entry->old_sysInfo2ProfileId;
                table_entry->old_sysInfo2ProfileId = 0;
                break;
            case COLUMN_NUMRAPREAMBLES:
                table_entry->numRaPreambles     = table_entry->old_numRaPreambles;
                table_entry->old_numRaPreambles = 0;
                break;
            case COLUMN_SIZERAPREAMBLESGROUPA:
                table_entry->sizeRAPreamblesGroupA     = table_entry->old_sizeRAPreamblesGroupA;
                table_entry->old_sizeRAPreamblesGroupA = 0;
                break;
            case COLUMN_MESSAGESIZEGROUPA:
                table_entry->messageSizeGroupA     = table_entry->old_messageSizeGroupA;
                table_entry->old_messageSizeGroupA = 0;
                break;
            case COLUMN_MESSAGEPOWEROFFSETGROUPB:
                table_entry->messagePowerOffsetGroupB     = table_entry->old_messagePowerOffsetGroupB;
                table_entry->old_messagePowerOffsetGroupB = 0;
                break;
            case COLUMN_POWERRAMPINGSTEP:
                table_entry->powerRampingStep     = table_entry->old_powerRampingStep;
                table_entry->old_powerRampingStep = 0;
                break;
            case COLUMN_PREAMBLEINITRCVDTGTPOWER:
                table_entry->preambleInitRcvdTgtPower     = table_entry->old_preambleInitRcvdTgtPower;
                table_entry->old_preambleInitRcvdTgtPower = 0;
                break;
            case COLUMN_PREAMBLETRANSMAX:
                table_entry->preambleTransMax     = table_entry->old_preambleTransMax;
                table_entry->old_preambleTransMax = 0;
                break;
            case COLUMN_RARSPWINDOWSIZE:
                table_entry->raRspWindowSize     = table_entry->old_raRspWindowSize;
                table_entry->old_raRspWindowSize = 0;
                break;
            case COLUMN_MACCONTRESOLUTIONTIMER:
                table_entry->macContResolutionTimer     = table_entry->old_macContResolutionTimer;
                table_entry->old_macContResolutionTimer = 0;
                break;
            case COLUMN_MAXHARQMSG3TX:
                table_entry->maxHarqMsg3Tx     = table_entry->old_maxHarqMsg3Tx;
                table_entry->old_maxHarqMsg3Tx = 0;
                break;
            case COLUMN_MODIFICATIONPERIODCOEFF:
                table_entry->modificationPeriodCoeff     = table_entry->old_modificationPeriodCoeff;
                table_entry->old_modificationPeriodCoeff = 0;
                break;
            case COLUMN_DEFAULTPAGCYCLE:
                table_entry->defaultPagCycle     = table_entry->old_defaultPagCycle;
                table_entry->old_defaultPagCycle = 0;
                break;
            case COLUMN_NB:
                table_entry->nB     = table_entry->old_nB;
                table_entry->old_nB = 0;
                break;
            case COLUMN_ROOTSEQUENCEINDEX:
                table_entry->rootSequenceIndex     = table_entry->old_rootSequenceIndex;
                table_entry->old_rootSequenceIndex = 0;
                break;
            case COLUMN_PRACHCONFIGINDEX:
                table_entry->prachConfigIndex     = table_entry->old_prachConfigIndex;
                table_entry->old_prachConfigIndex = 0;
                break;
            case COLUMN_HIGHSPEEDFLAG:
                table_entry->highSpeedFlag     = table_entry->old_highSpeedFlag;
                table_entry->old_highSpeedFlag = 0;
                break;
            case COLUMN_ZEROCORRELATIONZONECONFIG:
                table_entry->zeroCorrelationZoneConfig     = table_entry->old_zeroCorrelationZoneConfig;
                table_entry->old_zeroCorrelationZoneConfig = 0;
                break;
            case COLUMN_PUSCHFREQHOPPINGSUBBANDS:
                table_entry->puschFreqHoppingSubBands     = table_entry->old_puschFreqHoppingSubBands;
                table_entry->old_puschFreqHoppingSubBands = 0;
                break;
            case COLUMN_PUSCHFREQHOPPINGMODE:
                table_entry->puschFreqHoppingMode     = table_entry->old_puschFreqHoppingMode;
                table_entry->old_puschFreqHoppingMode = 0;
                break;
            case COLUMN_PUSCHFREQHOPPINGOFFSET:
                table_entry->puschFreqHoppingOffset     = table_entry->old_puschFreqHoppingOffset;
                table_entry->old_puschFreqHoppingOffset = 0;
                break;
            case COLUMN_ULRSGROUPHOPPINGENABLED:
                table_entry->ulRsGroupHoppingEnabled     = table_entry->old_ulRsGroupHoppingEnabled;
                table_entry->old_ulRsGroupHoppingEnabled = 0;
                break;
            case COLUMN_ULRSGROUPPUSCH:
                table_entry->ulRsGroupPusch     = table_entry->old_ulRsGroupPusch;
                table_entry->old_ulRsGroupPusch = 0;
                break;
            case COLUMN_ULRSSEQHOPPINGENABLED:
                table_entry->ulRsSeqHoppingEnabled     = table_entry->old_ulRsSeqHoppingEnabled;
                table_entry->old_ulRsSeqHoppingEnabled = 0;
                break;
            case COLUMN_ULRSCYCLICSHIFTPUSCH:
                table_entry->ulRsCyclicShiftPusch     = table_entry->old_ulRsCyclicShiftPusch;
                table_entry->old_ulRsCyclicShiftPusch = 0;
                break;
            case COLUMN_DELTAPUCCHSHIFT:
                table_entry->deltaPucchShift     = table_entry->old_deltaPucchShift;
                table_entry->old_deltaPucchShift = 0;
                break;
            case COLUMN_N1PUCCHAN:
                table_entry->n1PucchAn     = table_entry->old_n1PucchAn;
                table_entry->old_n1PucchAn = 0;
                break;
            case COLUMN_SRSBANDWIDTHCONFIG:
                table_entry->srsBandwidthConfig     = table_entry->old_srsBandwidthConfig;
                table_entry->old_srsBandwidthConfig = 0;
                break;
            case COLUMN_SRSSUBFRMCONFIG:
                table_entry->srsSubfrmConfig     = table_entry->old_srsSubfrmConfig;
                table_entry->old_srsSubfrmConfig = 0;
                break;
            case COLUMN_SRSSIMULTANEOUSANSRSTX:
                table_entry->srsSimultaneousAnSrsTx     = table_entry->old_srsSimultaneousAnSrsTx;
                table_entry->old_srsSimultaneousAnSrsTx = 0;
                break;
            case COLUMN_PONOMINALPUSCH:
                table_entry->poNominalPusch     = table_entry->old_poNominalPusch;
                table_entry->old_poNominalPusch = 0;
                break;
            case COLUMN_DELTAFPUCCHFORMAT1:
                table_entry->deltaFPucchFormat1     = table_entry->old_deltaFPucchFormat1;
                table_entry->old_deltaFPucchFormat1 = 0;
                break;
            case COLUMN_DELTAFPUCCHFORMAT1B:
                table_entry->deltaFPucchFormat1b     = table_entry->old_deltaFPucchFormat1b;
                table_entry->old_deltaFPucchFormat1b = 0;
                break;
            case COLUMN_DELTAFPUCCHFORMAT2:
                table_entry->deltaFPucchFormat2     = table_entry->old_deltaFPucchFormat2;
                table_entry->old_deltaFPucchFormat2 = 0;
                break;
            case COLUMN_DELTAFPUCCHFORMAT2A:
                table_entry->deltaFPucchFormat2a     = table_entry->old_deltaFPucchFormat2a;
                table_entry->old_deltaFPucchFormat2a = 0;
                break;
            case COLUMN_DELTAFPUCCHFORMAT2B:
                table_entry->deltaFPucchFormat2b     = table_entry->old_deltaFPucchFormat2b;
                table_entry->old_deltaFPucchFormat2b = 0;
                break;
            case COLUMN_DELTAPREAMBLEMSG3:
                table_entry->deltaPreambleMsg3     = table_entry->old_deltaPreambleMsg3;
                table_entry->old_deltaPreambleMsg3 = 0;
                break;
            case COLUMN_T300:
                table_entry->t300     = table_entry->old_t300;
                table_entry->old_t300 = 0;
                break;
            case COLUMN_T301:
                table_entry->t301     = table_entry->old_t301;
                table_entry->old_t301 = 0;
                break;
            case COLUMN_T310:
                table_entry->t310     = table_entry->old_t310;
                table_entry->old_t310 = 0;
                break;
            case COLUMN_N310:
                table_entry->n310     = table_entry->old_n310;
                table_entry->old_n310 = 0;
                break;
            case COLUMN_T311:
                table_entry->t311     = table_entry->old_t311;
                table_entry->old_t311 = 0;
                break;
            case COLUMN_N311:
                table_entry->n311     = table_entry->old_n311;
                table_entry->old_n311 = 0;
                break;
            case COLUMN_ADDITIONALSPECTRUMEMISSION:
                table_entry->additionalSpectrumEmission     = table_entry->old_additionalSpectrumEmission;
                table_entry->old_additionalSpectrumEmission = 0;
                break;
            case COLUMN_TIMEALIGNMENTTIMER:
                table_entry->timeAlignmentTimer     = table_entry->old_timeAlignmentTimer;
                table_entry->old_timeAlignmentTimer = 0;
                break;
            case COLUMN_NRBCQI:
                table_entry->nRbCqi     = table_entry->old_nRbCqi;
                table_entry->old_nRbCqi = 0;
                break;
            case COLUMN_ENABLE64QAM:
                table_entry->enable64QAM     = table_entry->old_enable64QAM;
                table_entry->old_enable64QAM = 0;
                break;
            case COLUMN_NCSAN:
                table_entry->nCsAn     = table_entry->old_nCsAn;
                table_entry->old_nCsAn = 0;
                break;
            }
        }
#endif /* PWAV_SIH */
        break;

    case MODE_SET_COMMIT:
        break;
    }
    return SNMP_ERR_NOERROR;
}
